# FAST-MVP 配对结论与后续架构取舍

状态：NOT_EXECUTED。原始逐条记录见 `../experiments/fast-mvp-runs.jsonl`；目前没有正式 A/B 结果。H0 和聊天示例不计入。

R3 后先按 A1/A2/A3/B1/B2/B3 列逐条事实，再汇总计划数、实际开始数、完成数、无开发者介入完成数、技术成功数、Owner 接受可交付数、失败/阻断/未执行数。失败、超时和救场不得删除。

比较人工持续介入、人物/商品切换、输入/任务/作品关联、防重/费用、飞影等待之外耗时，并分辨飞影内容、工作台、环境或 UNKNOWN 原因。Agent 点击次数不是人类劳动时间；首次安装/部署/排障单列；三条样片不能证明质量优劣或长期可靠性。

候选：A 保留现有工作台并隐藏无价值步骤；B 薄工作台复用同一核心；C 简洁默认流程加按需高级控制。当前不选择，不另建 C 测试组。

每项建议必须列出：观察证据、用户影响、预期收益、最小改动、回归范围、回滚方法。明确缺陷可在适用授权内修复；重大产品/架构变更由 Owner 决定。
