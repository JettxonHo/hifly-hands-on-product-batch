# 产品决策日志

> 状态：Accepted
> Owner：owner（JettxonHo）
> 最后更新：2026-08-04
> 适用范围：全部产品与架构决策的权威记录；文档冲突时以本文件与 owner 最新决策为准
> 非目标：本文件不记录实现细节；新决策以追加条目方式记录，不覆盖历史

每条决策包含：日期、决策、原因、影响、被否决方案、后续可重新评估条件。

---

## D-001 产品从飞影批量工具升级为 AI 数字人内容生产 SaaS

- **日期**：2026-08-04
- **决策**：正式产品定位升级为「面向电商运营团队的 AI 数字人内容生产 SaaS」，覆盖商品与营销目标、文案生成与质检、数字人/声音/背景素材、视频方案审核、批量生产、成片质检、交付与发布管理的完整工作流。
- **原因**：用户购买的是生产结果而非网页自动化；仅作为飞影批量工具会限制产品边界与商业空间。
- **影响**：新建 `docs/product/` 产品文档体系；后续 Epic 按 SAAS-001 系列拆分；产品语言全面转向运营视角。
- **被否决方案**：继续维持「飞影网页批量自动化工具」定位，只做功能增强。
- **可重新评估条件**：目标市场或商业模式发生根本变化，由 owner 重新决策。

## D-002 飞影作为 Provider，不作为上层领域模型

- **日期**：2026-08-04
- **决策**：飞影只是底层视频生成供应商之一；上层领域模型不使用飞影按钮文案、页面 selector 或具体网页步骤作为领域概念。
- **原因**：产品上层与单一供应商页面结构永久绑定会导致不可维护与不可替换。
- **影响**：引入 Provider Adapter 抽象（Hifly Playwright/API、影刀可选、其他平台）与 capability 模型。
- **被否决方案**：以飞影页面流程直接作为产品领域模型。
- **可重新评估条件**：飞影正式开放完整 API 且成为唯一供应商时，可评估简化 Adapter 层（仍需保留抽象）。

## D-003 保留并下沉现有可靠执行内核

- **日期**：2026-08-04
- **决策**：现有批量执行内核（状态机、原子写、幂等、证据、CI 标准）保留并下沉为 Local Execution Agent / Provider Execution Engine，不推翻、不删除。
- **原因**：执行内核是已验证的资产；重写将引入不可控风险并浪费既有稳定性投入。
- **影响**：batch/task 保留为执行层工作单；产品层通过编译边界（compileApprovedVideoPlansToBatch）与执行层衔接。
- **被否决方案**：推倒重来，用新执行系统替换现有内核。
- **可重新评估条件**：执行内核出现无法修复的架构性限制，且替代方案有充分验证。

## D-004 采用云端 Control Plane + Local Agent

- **日期**：2026-08-04
- **决策**：架构采用云端 SaaS Control Plane 与本地/VPS Local Agent 分离；长时 Playwright 任务不进入 Serverless/Workers 请求生命周期。
- **原因**：浏览器长任务、本地文件、登录态与人工接管都要求常驻本地执行环境；云端适合编排、状态与商业信息。
- **影响**：AGENT-001 规划 Agent 协议；云端不保存不必要的飞影 Cookie。
- **被否决方案**：纯云端 Serverless 执行 Playwright；纯本地单机产品（无云端控制面）。
- **可重新评估条件**：Provider 全面开放 API 且无需浏览器执行时，可评估 Agent 轻量化。

## D-005 产品主流程采用五阶段

- **日期**：2026-08-04
- **决策**：统一阶段导航固定为：1 商品与目标 → 2 文案与质检 → 3 人物与素材 → 4 视频方案 → 5 生成与交付。
- **原因**：五阶段与运营的真实生产过程对齐，是产品语言与技术执行之间的稳定边界。
- **影响**：项目内导航、领域模型分组、用户流程文档全部按五阶段组织。
- **被否决方案**：按技术模块（上传/执行/下载）组织主流程；自由画布式无阶段流程。
- **可重新评估条件**：用户研究发现阶段划分显著违背目标用户心智，由 owner 决策调整。

## D-006 第一阶段不自研数字人基础模型

- **日期**：2026-08-04
- **决策**：第一阶段通过 Provider 完成数字人/声音/视频能力（公共数字人、照片数字人、复刻、AI 生成、声音克隆、对口型、换背景等），不自研基础模型。
- **原因**：基础模型训练成本与周期远超产品当前阶段；上层价值在资产建模、编排、审核与交付。
- **影响**：SaaS 层聚焦资产建模/选择/配置/编排/跟踪/登记/审核/交付；能力可用性受 Provider capability 约束。
- **被否决方案**：自研数字人生成模型作为第一阶段核心。
- **可重新评估条件**：商业规模与团队能力达到模型投入门槛，由 owner 重新决策。

## D-007 影刀仅作为可选 Adapter

- **日期**：2026-08-04
- **决策**：影刀可作为可选 Adapter，但不能成为产品领域模型和核心执行协议的唯一基础；产品主链路不绑定只有 Windows 能稳定运行的低代码 RPA。
- **原因**：跨平台（macOS/Windows/后续 Linux）是产品竞争力；单一 RPA 依赖带来平台与供应商双重锁定。
- **影响**：核心执行协议自建（Agent 协议 + Provider Adapter）；影刀仅在特定场景补充。
- **被否决方案**：以影刀为唯一执行器快速搭产品。
- **可重新评估条件**：影刀提供跨平台稳定执行与正式集成契约，可评估提升其适配层级（仍非唯一）。

## D-008 运营产品语言与技术执行状态分离

- **日期**：2026-08-04
- **决策**：运营界面使用生产结果语言（待生成/生成中/已完成/需人工处理等）；技术状态（Playwright、batch.json、executor、snapshot、queue、lock、capture HTTP 等）收敛到任务详情、诊断页面与本地执行器页面。
- **原因**：运营人员购买生产结果，不理解也不应被要求理解执行内部机制。
- **影响**：增加业务状态映射层；信息架构中运营区与技术区隔离；内部 pointBudget 不作为用户术语。
- **被否决方案**：直接把底层状态机状态展示给运营用户。
- **可重新评估条件**：无（原则性决策）；映射层细节可迭代。

## D-009 VideoPlan 是业务计划，batch/task 是执行工作单

- **日期**：2026-08-04
- **决策**：新产品层中 VideoPlan 是业务真相；现有 batch/task 是执行层工作单；batch 不直接暴露为用户项目，用户不修改底层 execution 状态；两者经编译边界衔接并建立双向追踪 ID。
- **原因**：业务计划需要版本、审核、变体等语义；执行工作单需要原子、幂等、证据等语义；二者混用一个模型会互相破坏。
- **影响**：规划 compileApprovedVideoPlansToBatch() 编译边界；领域模型多状态域分离。
- **被否决方案**：直接扩展 batch 结构承载全部业务语义；或废弃 batch 另建执行系统。
- **可重新评估条件**：编译边界实现后发现追踪模型不足，可在不改变「业务/执行分离」前提下调整实现。

## D-010 产品化与底层稳定性工作可以并行

- **日期**：2026-08-04
- **决策**：SaaS 产品化（SAAS-001 系列）与底层稳定性工作（CORE/CI 系列）可并行推进，但互不搭车：各自独立分支、独立 PR、独立审查。
- **原因**：稳定性是产品化的前提之一，等待串行完成会拖慢整体；搭车合并会破坏 PR 可审查性。
- **影响**：PR 治理保持「独立分支、无搭车合并」；产品 PR 不修改执行内核，稳定性 PR 不引入产品概念。
- **被否决方案**：先完全结束稳定性工作再开始产品化；或在同一 PR 混合两类变更。
- **可重新评估条件**：无（流程性决策）；具体并行节奏由 owner 按资源调整。

## D-011 数字人、声音和人物源素材的授权与敏感资产保护底线

- **日期**：2026-08-04
- **决策**：用户照片、视频、声音和数字人复刻源素材属于敏感资产，产品必须满足以下底线：
  1. 未记录有效授权，不得：创建数字人、克隆声音、上传 Provider、创建对口型任务、创建生产任务；
  2. 授权失效、撤销或资产 disabled 后：新任务 Preflight 必须 fail-closed，不得继续创建新的 Provider 任务；
  3. 资产的创建、使用、撤销、删除需要审计留痕；
  4. 云端和 Local Agent 只处理任务需要的最小数据；
  5. 普通运营不能通过修改前端字段绕过授权状态；
  6. Provider Adapter 在真实上传前必须重新校验授权状态；
  7. 具体授权材料形式、保留期限、删除时限和法律文本仍为开放问题，保留在 Q-007 / Q-008，不得擅自决定；
  8. 资产可用状态（asset.status）与授权状态（authorization.status）分离建模，两者同时满足才能通过生产 Preflight。
- **原因**：数字人/声音复刻涉及人格权与合规风险；授权缺位下的生产与上传会造成不可逆的法律与信任损失，必须在产品模型层 fail-closed，而不是依赖运营自觉。
- **影响**：领域模型新增 AuthorizationRecord / ConsentEvidence / AuthorizationRevocation / DeletionRequest / AuditEvent 规划（见 [DOMAIN_MODEL.md](DOMAIN_MODEL.md) 第七节）；Preflight 增加授权校验项；Provider Adapter 上传前强制重校验；蓝图新增原则 6。
- **被否决方案**：仅在运营文档中提示授权重要性而不做系统强制；把授权材料与业务数据混合存储、事后补录。
- **可重新评估条件**：授权材料形式与法律文本（Q-007/Q-008）确定后可细化实现；「fail-closed + 审计留痕 + 最小数据」底线本身不重新开放。

## D-012 Provider Task Router：API Worker + Local Agent 双执行路径

- **日期**：2026-08-04
- **决策**：长期执行架构不要求所有任务都经过 Local Agent。引入 Provider Task Router：已确认 API 能力由 Hifly API Worker 处理；仅网页支持、登录态、本地文件和人工接管能力由 Local Agent / Playwright 处理。API 创作任务必须异步执行，不放在普通 HTTP 请求生命周期内。
- **原因**：飞影数字人 API V2 文档已确认 14 项能力在文档层面存在（账号权限与真实调用仍待确认）；API 能力在权限与验证具备后无需依赖浏览器自动化，而登录态、本地文件与人工接管能力仍必须留在 Local Agent。这是对 D-004 的细化而非推翻：长任务不进请求生命周期的约束保持不变。
- **影响**：`PROVIDER_AND_AGENT_ARCHITECTURE.md` 第四节（任务路由）与第六节（API 调研五层确认）更新；回调不能作为唯一完成机制，必须配合轮询对账（provider task id / request id / callback received time / last poll time / 主动对账）；Token 保管位置由 Q-018 决策，决策前不得默认上传云端。
- **被否决方案**：所有任务一律经过 Local Agent；API 创作任务同步放入请求生命周期。
- **可重新评估条件**：飞影 API 账号权限与真实调用验证结果决定 API Worker 承接能力的范围；「长任务不进请求生命周期」「Local Agent 保留仅网页/登录态/人工接管能力」原则不重新开放。

## D-013 Hifly-first Provider 策略（Hifly-first，不是 Hifly-only）

- **日期**：2026-08-04
- **决策**：正式表述为 **Hifly-first，不是 Hifly-only**。在飞影满足以下条件时，优先使用飞影完成数字人、声音、普通数字人口播、音频驱动、对口型、背景处理和相关视频能力：
  1. 功能覆盖满足产品需求；
  2. 真实效果质量达标；
  3. 自动化路径稳定；
  4. 成本和生成时延可接受；
  5. 能取得稳定 task ID、状态、结果和失败信息；
  6. 可安全重试、对账和恢复；
  7. 满足授权与敏感资产要求（D-011）。

  执行路径偏好顺序：

  ```text
  1. 飞影正式 API
  2. 飞影 Playwright
  3. 飞影人工接管的半自动流程
  4. 其他 Provider
  ```

  但这不是无条件强制：当其他 Provider 在关键功能、质量、稳定性、成本或合规上明显更优时，可以经相同 Provider Adapter 接入。**未经 capability 实际调研与验证，不得宣布飞影支持该能力。**
- **原因**：优先单一主 Provider 可以减少运营配置、减少数字人和声音资产迁移、减少多供应商 Token 与账号管理、保持人物、声音和作品链路一致；同时保留 Adapter 抽象避免 Hifly-only 锁定。
- **影响**：第一版普通运营界面**不要求用户选择 Provider**——用户选择的是生产结果类型，Provider Task Router 根据已验证能力路由；Provider 选择只进入高级设置、管理员配置或技术诊断区域。capability 确认表（`PROVIDER_AND_AGENT_ARCHITECTURE.md` 第九节）是路由与排期的依据。
- **被否决方案**：Hifly-only（禁止其他 Provider 接入）；多 Provider 完全对等、由运营在首版界面自由选择。
- **可重新评估条件**：飞影某能力不满足上述七项条件时，该能力可经 Adapter 接入其他 Provider；「Hifly-first 偏好 + Adapter 抽象不绑定」的框架本身不重新开放。

## D-014 企业内部目标用户与产品边界

- **日期**：2026-08-04
- **决策**：第一批目标用户是**企业内部的电商运营、内容生产、内容审核和管理团队**。产品属于**企业内部 AI 数字人内容生产平台**，不是面向公众自助购买的通用消费者 SaaS。第一版优先服务单一企业、单一组织，但领域模型保留 Organization 边界，不得把全部数据永久建模为无组织归属的全局数据。核心价值：企业内部项目与商品管理、文案生成/版本与审核、数字人/声音和敏感资产管理、视频方案审核、批量生产、失败处理、作品交付、内部用量/成本和审计。
- **原因**：企业内部场景可以聚焦生产链路与治理能力，避免公众自助购买场景的获客、支付与合规复杂度；同时保留组织边界为后续多组织扩展留余地。
- **影响**：**关闭 Q-001**；组织、权限、授权、审计仍然重要；产品不再以公众自助购买和套餐转化为设计中心。
- **被否决方案**：面向公众自助购买的通用消费者 SaaS；完全无组织归属的全局数据模型。
- **可重新评估条件**：owner 决定开放公众自助注册与购买时，重新评估产品边界与商业化范围（仍不与 D-016 的内部治理定位自动冲突，需另行决策）。
- **后续决策**：第一版企业内部登录与单组织进入方式已由 D-024 确定。

## D-015 Cloud-first，Tencent Cloud first

- **日期**：2026-08-04
- **决策**：第一版产品就是**可以通过浏览器访问的 Cloud-first Web Control Plane，默认部署目标为腾讯云**。用户通过云端 Web 产品完成：登录、项目管理、商品与文案、人物与素材、VideoPlan、审核、任务查看、作品管理。依赖网页登录态、本地文件、Playwright、下载和人工接管的能力，仍由 Local Agent 或常驻执行节点承担。正式表述：**Tencent Cloud first，不是 Tencent Cloud only**。要求：
  1. 业务领域模型不得依赖具体腾讯云产品名称；
  2. Database、ObjectStorage、Queue、SecretStore 使用基础设施抽象；
  3. 腾讯云只作为第一版默认部署实现；
  4. 具体腾讯云数据库、对象存储、队列、密钥管理和部署服务仍待 Q-021 决定；
  5. 不得在本轮选择具体腾讯云产品。
- **原因**：云端 Web 形态让企业内部团队通过浏览器即可协作使用；腾讯云是 owner 选定的第一版部署目标；领域层与具体云厂商解耦保持可迁移性。
- **影响**：**关闭 Q-002**（第一版提供云端 Web 登录）；**关闭 Q-013**（第一版先做单企业/单组织云端 MVP）；**收窄 Q-014**（云厂商确定为腾讯云，具体服务仍开放）；调整 DELIVERY_ROADMAP 的 Phase 顺序（云端 Control Plane 提前进入 Phase 1）。
- **被否决方案**：Local-first 桌面/单机优先形态；本轮直接选定具体腾讯云产品。
- **可重新评估条件**：Q-021 确定具体腾讯云服务后细化部署设计；「Cloud-first + 领域层不绑定具体云产品」框架本身不重新开放。**不得根据 owner 提到的 WenrongAI 推测其技术栈**——只记录 owner 明确选择的「腾讯云部署 + 云端 Web 产品形态」。
- **后续决策**：Cloud Web 的 MVP 登录方式已由 D-024 确定。

## D-016 企业内部使用，不建设支付和套餐

- **日期**：2026-08-04
- **决策**：产品主要供企业内部使用。当前产品路线**不建设**：支付、套餐、订阅、账单、充值、余额、优惠券、续费、面向客户的自动计费。**仍保留**：企业内部任务用量、Provider 消耗、LLM 调用量、成本估算、内部预算和限额、成员使用情况、审计日志、异常消耗告警。将现有 **COMMERCIAL-001** 统一调整为 **ENTERPRISE-001：企业权限、治理、内部用量与审计**。不得删除 UsageRecord、CostEstimate 等内部治理所需模型，但必须移除套餐、账单和付款语义。
- **原因**：企业内部平台的治理需求（用量、成本、限额、审计）与面向客户的收费系统（支付、套餐、账单）是两类不同问题；当前阶段只需要前者。
- **影响**：DELIVERY_ROADMAP 的 COMMERCIAL-001 改为 ENTERPRISE-001；DOMAIN_MODEL 移除 Plan/Subscription/Billing/Payment/Package/Balance 等纯收费实体或标记为非目标；保留并重新解释 UsageRecord/CostEstimate/Quota/Budget 为内部治理用途；信息架构移除套餐中心/支付/账单入口。
- **被否决方案**：保留完整商业化（支付/套餐/账单）作为第一版范围；删除全部用量与成本模型。
- **可重新评估条件**：owner 决定面向外部客户收费时，另行决策恢复商业化范围；「内部用量与成本治理保留」不重新开放。

## D-017 人物步骤同时支持创建新人物和选择现有人物

- **日期**：2026-08-04
- **决策**：产品的人物步骤统一表达为「**创建新人物或选择现有人物**」。产品入口至少规划：公共数字人、企业已有数字人、创建新人物。但第一条端到端黄金路径和图片数字人创建**必须拆开实施**。
  - **Vertical Slice A（已有/公共人物黄金路径）**：云端登录 → 创建项目 → 上传商品 → 生成和质检文案 → 选择公共或已有数字人 → 创建并审核 VideoPlan → 编译为现有 batch → 当前手里有货执行 → 作品库交付。
  - **Vertical Slice B（图片数字人创建）**：上传人物图片 → 检查有效授权 → 创建飞影图片数字人异步任务 → 查询或接收任务状态 → 获取 Provider avatar 标识 → 登记 AvatarAsset → 回到统一人物选择流程 → 用于 VideoPlan。

  Vertical Slice A / Vertical Slice B 是垂直切片标签，不是 DELIVERY_ROADMAP 的 Phase 1 / Phase 2 编号。

  要求：Vertical Slice A 不依赖 Vertical Slice B 完成；不得把图片数字人创建、授权、上传、异步任务和完整主链路塞进一个大 PR；创建人物必须继续遵守 D-011；未记录有效授权不得上传 Provider 或创建数字人；新人物创建可能产生 Provider 消耗，开发前必须获得 owner 对真实调用的单独授权。
- **原因**：黄金路径先用已有/公共人物打通端到端，风险最低；图片数字人创建涉及授权、异步任务与 Provider 消耗，独立切片实施可控。
- **影响**：**关闭 Q-016**，并将上述两层切片作为正式结论记录；USER_FLOWS 增加 Flow A（选择已有/公共人物黄金路径）与 Flow B（创建图片数字人）；DOMAIN_MODEL 规划 AvatarCreationTask；DELIVERY_ROADMAP Vertical Slice A/B 拆分。
- **被否决方案**：把图片数字人创建与黄金路径合并为一个大切片；人物步骤只支持选择或只支持创建。
- **可重新评估条件**：Vertical Slice A 交付后可启动 Vertical Slice B；D-011 授权底线与「真实调用需 owner 单独授权」不重新开放。

## D-018 飞影多能力是正式产品路线

- **日期**：2026-08-04
- **决策**：以下能力属于**正式产品路线**，不是可有可无的实验项：当前手里有货、公共数字人、公共/自有声音、普通文本数字人口播、音频驱动数字人口播、图片数字人创建、视频数字人复刻、声音克隆、通用对口型、视频背景替换、其他经过调研确认的飞影能力。但每项能力必须严格通过五层状态：1. API 文档已确认；2. 当前账号权限已确认；3. 真实调用已验证；4. Adapter 已实现；5. SaaS 产品能力已完成。**不得因为它属于正式路线，就提前宣布已经支持。** HIFLY-001 推荐调研和接入顺序：
  0. 保留当前手里有货 Playwright 能力
  1. 公共数字人列表
  2. 公共/自有声音列表
  3. 文本驱动普通数字人口播
  4. 音频驱动普通数字人口播
  5. 图片数字人创建
  6. 声音克隆
  7. 视频数字人复刻
  8. 通用对口型
  9. 视频背景替换
  10. 手里有货背景和场景来源

  这个顺序是当前规划优先级，不代表账号权限和真实调用已验证。
- **原因**：明确正式路线避免能力被当作实验项随意砍掉；同时五层状态与 Evidence 台账防止未验证能力被提前宣布。
- **影响**：**收窄但不关闭 Q-005**（产品目标和优先级已确定，第一批实际实现范围仍需结合账号权限与 HIFLY-001 Evidence 决定）；新增 [HIFLY_CAPABILITY_EVIDENCE.md](HIFLY_CAPABILITY_EVIDENCE.md) Evidence 台账按此顺序记录；DELIVERY_ROADMAP Phase 3 按此顺序逐项推进。
- **被否决方案**：把飞影多能力列为可选实验项；因属于正式路线而跳过五层确认直接宣布支持。
- **可重新评估条件**：账号权限与真实调用验证结果可调整接入顺序；「五层状态 + Evidence 先行」原则不重新开放。

## D-019 文案生成与质检属于 SaaS 自有业务能力

- **日期**：2026-08-04
- **决策**：文案生成、版本管理、品牌规则、质检和审核属于 **SaaS 自有业务能力**，不得永久依赖飞影网页内置文案功能。架构采用：
  ```text
  CopyGenerationService
          ↓
  LLM Provider Adapter
  ```
  至少预留：平台默认模型、企业自有 API Key、可替换 Provider、可配置 base URL、可配置 model。普通运营用户只看到「生成文案」和业务配置，不要求其理解 LLM Provider、base URL 或 API Key。飞影内置文案功能可以作为：效果对比参考、特定模板辅助能力、未来经过正式接口验证后的可选 CopyProvider——但不能成为阶段二业务真相。
- **原因**：文案是阶段二的业务真相，必须掌握在 SaaS 自有能力中；依赖飞影网页内置功能会让核心业务受制于供应商页面变化。
- **影响**：**Q-003 收窄为**：LLM Adapter 架构已决定；默认 Provider 和默认模型仍待 Q-019 决定。**Q-004 保持开放**：规则、品牌规范、LLM 语义评估和人工审核的权重仍未决定。DOMAIN_MODEL 规划 LlmProviderConfig；PROVIDER_AND_AGENT_ARCHITECTURE 增加 LLM Provider Adapter 分层。
- **被否决方案**：永久依赖飞影网页内置文案功能作为阶段二业务真相；本轮即选定具体 LLM Provider/模型。
- **可重新评估条件**：Q-019 确定默认 Provider/模型后细化实现；「文案为 SaaS 自有能力 + LLM Adapter 抽象」框架不重新开放。

## D-020 MVP 平台默认凭证，后续企业 BYOK

- **日期**：2026-08-04
- **决策**：MVP 阶段使用 owner 在部署环境中安全配置的**平台默认 LLM API Key**。产品文档和代码中统一称为「**平台默认凭证**」。不得把领域模型永久绑定到个人身份或「owner 私人 Key」。MVP 普通用户无需配置 API Key 即可使用文案功能。后续企业版本：企业管理员可以在高级设置中选择使用平台默认模型或使用企业自有 API Key。**企业 BYOK 是组织级配置，不是每个普通运营用户单独配置。** 安全底线：
  1. 真实 Key 不进入前端代码；
  2. 真实 Key 不提交 GitHub；
  3. 真实 Key 不写入 Markdown；
  4. 真实 Key 不出现在日志；
  5. 真实 Key 不出现在错误信息；
  6. 真实 Key 不进入证据截图；
  7. 浏览器不得读取完整 Key；
  8. Local Agent 无必要时不得获得 LLM Key；
  9. 页面只显示掩码；
  10. 只有企业管理员可新增、更新、测试和撤销企业 Key；
  11. Key 使用加密 Secret 引用；
  12. 审计记录谁在何时修改配置；
  13. 任务只记录 credential configuration/version ID，不记录 Key。

  第一版领域模型可以规划 LlmProviderConfig（organizationId、provider、baseUrl、model、credentialSource: platform | organization、encryptedSecretRef、status、createdBy、updatedBy、lastConnectionTestAt）。**本轮只做文档规划，不实现 Key 存储。** 新增开放问题：Q-019（MVP 默认 LLM Provider 和模型）、Q-020（企业 BYOK 上线阶段与允许的 Provider）、Q-021（腾讯云具体基础设施选型）、Q-022（第一版企业登录方式）。
- **原因**：MVP 用平台默认凭证让普通用户零配置使用文案能力；企业 BYOK 满足后续企业自有模型诉求；安全底线防止 Key 泄露。
- **影响**：DOMAIN_MODEL 规划 LlmProviderConfig；INFORMATION_ARCHITECTURE 设置区规划「AI 模型」（平台默认模型/企业自有 API Key）；PROVIDER_AND_AGENT_ARCHITECTURE 区分 LLM Key 与 Hifly Token 两类凭证；OPEN_QUESTIONS 新增 Q-019～Q-022。
- **被否决方案**：MVP 即要求每个用户配置自己的 API Key；把领域模型绑定到 owner 私人 Key；本轮实现 Key 存储。
- **可重新评估条件**：Q-019/Q-020 决定后细化 Provider 与 BYOK 实现；「真实 Key 安全底线 + 平台默认凭证/企业 BYOK 两级模型」不重新开放。
- **后续决策**：具体默认 Provider、模型和运行契约已由 D-023 确定。

## D-021 MVP 商品事实与 AI 文案生成门禁

- **日期**：2026-08-04
- **背景（Context）**：数字人手持商品种草视频可能存在两类用户输入：
  1. 用户已经有可用口播文案；
  2. 用户只有商品资料，需要平台生成文案。

  MVP 不应强制企业用户在创建商品前准备完整文案，但 AI 也不能在商品事实不足时自行编造卖点和产品信息。
- **决策**：
  1. **MVP 允许商品文案为空。**
  2. 用户可以根据结构化商品信息与内容目标生成口播文案。
  3. **AI 文案生成的最低商品输入**为：
     - 商品名称；
     - 至少一张商品图片；
     - 至少一条经用户确认的核心卖点。
  4. 商品描述和已有文案为**可选输入**。
  5. 商品图片主要服务后续数字人手持商品视频生产，**不能仅凭图片自动形成未经用户确认的正式商品事实**。
  6. 图片识别能力未来可以提供候选信息，但**必须经过用户确认后才能进入商品事实集合**。
  7. **模型只能基于已确认事实进行营销表达**，不得自行补充未经确认的：功效、参数、成分、认证或奖项、销量、排名、价格、优惠、库存、活动期限、竞品对比结论、医疗或健康承诺、其他未经用户确认的事实性信息。
  8. **所有 AI 生成文案都是草稿。**
  9. 文案必须经过**质检和人工确认**，才能被 VideoPlan 引用。
  10. 已有文案和 AI 生成文案进入**统一的 CopyVariant 审核流程**（已有文案可直接作为草稿，或经 AI 优化/改写后成为新草稿，同样经质检和人工确认后进入 VideoPlan）。
  11. **「文案为空」与「商品资料不满足生成条件」是两个不同状态**：不得把「文案为空」解释为「商品资料一定不完整」。
- **影响（Consequences）**：
  - Product 可以在没有文案时创建和保存；
  - 「生成文案」按钮必须受最低输入门禁控制；
  - 缺少最低输入时，不得调用 LLM；
  - 商品事实满足要求但文案为空时，可以调用文案生成服务；
  - AI 输出不能直接进入 VideoPlan；
  - 人工确认是进入 VideoPlan 前的强制门禁；
  - 质检的具体规则、权重和自动化范围仍由 Q-004 管理；
  - ContentBrief 的具体必填字段和默认值尚未由 owner 决定；
  - 本 Decision 不选择具体 LLM Provider 或模型；
  - Q-019 在本轮不得关闭。
- **被否决方案**：
  - 强制用户在创建商品时必须填写完整文案；
  - 只上传一张商品图片便允许模型自由生成全部卖点；
  - 将模型猜测的包装信息直接当作商品事实；
  - AI 文案生成后自动进入 VideoPlan；
  - 允许模型自行补充价格、功效、认证或销量信息。
- **可重新评估条件**：只有在以下情况发生时重新评估：
  - 企业明确要求完全无人工审核的自动生产；
  - 已建立可靠的商品知识库或商品主数据同步；
  - 图片识别具备可审计的用户确认流程；
  - 法务和业务确认了特定类目的自动事实抽取规则；
  - ContentBrief 和企业品牌规则已经形成独立正式决策。
- **后续决策**：ContentBrief 的 MVP 必填规则与默认行为已由 D-022 确定。
- **后续决策**：文案生成使用的 MVP 默认 Provider 已由 D-023 确定；D-021 商品事实和审核门禁继续有效。

## D-022 MVP ContentBrief 可选输入与默认生成行为

- **日期**：2026-08-04
- **背景（Context）**：数字人手持商品种草视频的核心生成门禁已经由 D-021 确定：商品名称、至少一张商品图片、至少一条经用户确认的核心卖点。ContentBrief 只用于描述「文案如何表达」，不应成为「是否允许生成文案」的第二套强制门禁。MVP 如果要求用户填写目标平台、目标人群、视频时长、表达风格、种草角度和 CTA，会增加录入成本，并容易形成庞大的平台及人群标签体系。同时，当前没有证据证明「手里有货」能力可以精确控制最终视频时长，因此不得把期望时长包装成成片时长承诺。
- **决策**：
  1. MVP 的 ContentBrief **不设置必填字段**。
  2. ContentBrief 可以完全不存在；可以存在但所有可选项均未填写；也可以只填写其中任意一个可选项。
  3. ContentBrief 为空时，**不得阻止 AI 文案生成**。
  4. AI 文案生成资格仍完全由 D-021 的最低商品事实门禁决定。
  5. **目标平台不作为 MVP 独立结构化字段。**
  6. **目标人群不作为 MVP 独立结构化字段。**
  7. MVP 不建立平台下拉列表、平台分类体系、人群标签库或平台与受众必选流程。
  8. 提供可选的「**补充要求**」自由文本，用于描述特殊受众、发布渠道、表达限制、语言要求、禁用措辞和其他创作说明。
  9. 「补充要求」中的事实性声明不能自动成为已确认商品事实，也不能绕过 D-021 的事实安全门禁。
  10. **表达风格**为可选项：默认「自然口语化种草」；支持预设；支持用户自定义；除默认值外，本 Decision 不固定完整预设目录。
  11. **种草角度**为可选项：默认由 AI 根据已确认商品事实和核心卖点选择；用户可以指定；用户可以自定义；不得通过选择种草角度补充未经确认的商品事实。
  12. **期望口播长度**为可选提示：只控制文案篇幅；不承诺最终视频时长；不保证最终成片与指定秒数精确一致；不作为生成门禁；本 Decision 不固定默认秒数、默认字数或默认长度档位；用户不填写时不得阻止文案生成。
  13. 产品界面不以 CTA 作为主要用户术语，统一命名为「**收尾方式**」。
  14. **收尾方式**为可选项：默认「自然收尾」；可选「轻度引导」；可选「无收尾引导」；可选用户自定义。
  15. 自定义收尾或补充要求中涉及价格、优惠、库存、活动期限、赠品、试用装、排名、销量、功效、参数、认证、医疗或健康结论等事实时，仍必须来自用户确认的商品事实；不得自行编造其他未经确认的声明。
  16. 所有默认生成、用户自定义、AI 改写与补充要求，继续受 D-021 事实安全规则约束。
  17. AI 生成结果仍为 CopyVariant 草稿，经过质检和人工确认后才能进入 VideoPlan。
- **影响（Consequences）**：
  - 用户完成 D-021 最低商品事实后，可以直接生成文案；
  - ContentBrief 不增加新的前置必填门禁；
  - 页面主流程应保持轻量；可选内容设置可以折叠或作为次级区域；
  - 系统可以应用表达风格、种草角度和收尾方式的默认行为；
  - 目标平台和目标人群不会进入 MVP 的独立字段模型；特殊平台或受众要求通过补充要求表达；
  - 期望口播长度只是文案篇幅提示；最终成片时长仍由实际脚本、语速和 Provider 结果决定；
  - ContentBrief 为空不属于错误；ContentBrief 的存在与 Product 是否满足生成条件是两种不同状态；
  - D-021 保持有效；Q-004 保持 OPEN；Q-019 保持 OPEN；
  - 本 Decision 不选择 LLM Provider；不实现飞影时长控制；不定义数据库字段或 API Schema。
- **被否决方案**：
  - 要求用户必须选择目标平台；
  - 要求用户必须选择目标人群；
  - 建设完整平台枚举后才能生成文案；
  - 建设完整人群标签体系后才能生成文案；
  - 强制用户填写视频秒数；
  - 向用户承诺最终成片一定满足指定时长；
  - 强制填写 CTA；
  - 使用英文 CTA 作为主要产品字段名称；
  - ContentBrief 为空时禁止生成文案；
  - 允许补充要求或自定义收尾绕过商品事实确认；
  - 默认生成未经用户确认的价格、优惠或功效信息。
- **可重新评估条件**：只有在以下情况发生时重新评估：
  - 产品正式提供平台专属文案适配；
  - 产品正式提供自动发布或平台合规规则；
  - 企业需要结构化受众分群和复用；
  - 已验证 Provider 可以稳定精确控制视频时长；
  - 数据分析需要将收尾方式作为结构化实验维度；
  - 企业品牌模板需要固定表达风格和收尾策略；
  - 法务确认特定事实可以从企业商品主数据自动同步；
  - MVP 进入多平台运营和投放分析阶段。
- **后续决策**：ContentBrief 的默认行为由 D-022 管理；其 LLM 执行 Provider 由 D-023 确定。

## D-023 MVP DeepSeek 官方 LLM Provider 与生成运行契约

- **日期**：2026-08-04
- **背景（Context）**：D-020 已决定 MVP 使用平台默认 LLM 凭证、企业零配置，但尚未选择具体 Provider 和模型（Q-019 保持开放）。D-021 已确定商品事实与文案安全门禁；D-022 已确定 ContentBrief 的可选输入和默认行为。MVP 需要一个无需企业配置、成本和延迟可控、支持结构化 JSON 输出的默认文案生成模型。需要明确官方 API 与第三方中转之间的数据边界；需要避免业务领域直接绑定某个 Provider；需要处理 JSON Output 偶发空内容和非法结构。Q-019 因此需要由本 Decision 正式解决。

  DeepSeek 官方文档只读核验（2026-08-04，未执行任何真实 API 调用）：官方 OpenAI 兼容 Base URL 为 `https://api.deepseek.com`；当前官方模型为 `deepseek-v4-flash` 与 `deepseek-v4-pro`，均支持非思考与思考模式（思考模式默认开启）并支持 JSON Output；旧模型名 `deepseek-chat` / `deepseek-reasoner` 已于 2026-07-24 停止使用；显式关闭思考模式为 `thinking.type = disabled`；JSON Output 要求 `response_format = {"type": "json_object"}`、prompt 中出现 json 字样、提供期望 JSON 结构示例并合理设置 `max_tokens`，且官方提示 JSON Output 偶尔可能返回空 content；官方错误文档列出 429 / 500 / 503。本轮不固化具体价格。
- **决策**：
  1. **MVP 使用 DeepSeek 官方开放平台作为默认 LLM Provider。**
  2. **API Key 必须由 DeepSeek 官方开放平台直接签发**；不使用第三方 API 中转平台、第三方模型聚合平台、第三方兼容网关，或冒充 DeepSeek 官方模型的代理服务。
  3. **平台使用平台管理的服务端凭证**；MVP 不要求企业用户自行配置 API Key；企业 BYOK 不进入 MVP，保留为后续版本能力（D-020 / Q-020）。
  4. **使用 DeepSeek 官方 OpenAI 兼容接口。**
  5. **Base URL 为 `https://api.deepseek.com`**；不使用第三方 Base URL、临时实验地址、已过期版本地址；本轮不以 Anthropic 兼容接口作为默认方案。本 Decision 不实现真实请求。
  6. **MVP 默认模型为 `deepseek-v4-flash`。**
  7. **不使用已停止的旧别名 `deepseek-chat` / `deepseek-reasoner`。**
  8. **`deepseek-v4-pro` 不作为 MVP 默认模型**，不自动 fallback、不自动升级；仅保留为未来质量评测候选、复杂改写候选、人工评估候选与后续可配置升级方案；MVP 不提供 V4-Flash / V4-Pro 用户界面切换器。
  9. **MVP 的批量商品口播文案生成显式使用非思考模式**，逻辑配置表达为 `thinking.type = disabled`；不得依赖 Provider 当前或未来的默认值。原因：当前任务是约束明确的商品文案生成；降低不必要的延迟；降低不必要的输出消耗；避免处理 reasoning_content；提高批量生成行为的一致性。不得声称思考模式永远没有价值；复杂任务是否采用思考模式留待后续质量评测。
  10. **架构隔离**：业务领域不得直接依赖 DeepSeek SDK、DeepSeek HTTP 请求结构、`deepseek-v4-flash` 字符串、reasoning_content 或 DeepSeek 专属错误格式；CopyGenerationService 只能通过 LLM Provider Adapter 调用 DeepSeek。调用链保持为：

      ```text
      D-021 Confirmed Product Facts
      + D-022 Optional ContentBrief
      → CopyGenerationPreflight
      → ContentBrief Normalization
      → CopyGenerationService
      → LLM Provider Adapter
      → DeepSeek Official API
      → CopyVariant Draft
      → Quality Gate
      → Human Approval
      → VideoPlan
      ```

      只有 LLM Provider Adapter 可以了解 DeepSeek Base URL、DeepSeek 模型名、thinking 参数、response_format、Provider 响应结构和 Provider 错误映射。Provider、Base URL、模型名和思考模式必须通过服务端配置管理；本 Decision 不固定具体环境变量名称。
  11. **Provider 配置和 Secret 只存在于服务端**：DeepSeek API Key 只能存在于服务端环境变量，或后续云基础设施决定（Q-021）采用的 Secret 管理服务。API Key 不得提交到 Git、写入 Markdown、写入示例真实值、发送到浏览器、存入 Product / Project / CopyVariant 等业务表、返回给客户端、出现在错误信息、日志、分析事件、截图、PR body、测试 fixture 或生成文案结果中。不得要求 owner 在聊天、Issue、PR 或终端输出中粘贴真实 Key。
  12. **文案生成使用 DeepSeek JSON Output 返回结构化结果**：设置 `response_format = {"type": "json_object"}`；在 prompt 中明确出现 json 字样；提供目标 JSON 结构示例；合理设置 `max_tokens` 以降低输出截断风险。
  13. **Provider 负责输出 JSON 对象，服务端负责业务校验**：DeepSeek 不原生执行业务 JSON Schema 校验；SaaS 服务端必须负责 JSON 解析、必需字段检查、字段类型检查、业务 Schema 校验、文案内容完整性检查、D-021 商品事实安全检查与 D-022 ContentBrief 约束检查；只有通过服务端校验的内容才能保存为 CopyVariant 草稿。
  14. **受控重试**：content 为空、JSON 无法解析、JSON 明显被截断或返回结构不满足服务端业务 Schema 时，MVP 允许使用同一个官方 Provider、同一个默认模型执行**最多一次**受控重试（首次请求 → 输出校验失败 → 最多一次同模型受控重试 → 再次失败则任务失败）。不得无限重试、静默循环、自动切换到 V4-Pro、自动切换其他 Provider、将不完整 JSON 保存为 CopyVariant、将部分文案当作成功结果，或因重试绕过事实安全检查。本 Decision 不固定完整 HTTP 错误重试矩阵、429/500/503 的具体退避时长、timeout 秒数、queue 参数或 circuit breaker 参数；这些属于后续实现规格。
  15. **重试仍失败时生成任务标记失败**：不创建有效 CopyVariant；向用户显示可理解的失败状态；由用户手动重新发起生成。
  16. **MVP 不自动切换模型或 Provider**：Flash → Pro、DeepSeek → 其他 Provider、非思考 → 思考、官方 API → 第三方中转，都不允许自动发生。原因：避免不可预测的成本变化；避免文案风格和质量突然变化；避免数据边界发生隐式变化；保持生成结果可追踪。未来如需多 Provider 容灾，必须形成新的 Owner Decision。
  17. **数据边界**：文案生成请求只能包含 D-021 已确认的文字商品事实、D-022 规范化后的可选 ContentBrief、生成任务需要的系统指令和业务输出结构说明。不得向 DeepSeek 发送 API Key 之外的其他 Secret、用户本地绝对路径、飞影 Cookie、Hifly Token、浏览器 Session、未确认的图片识别候选或未经确认的商品事实。商品图片虽然是 D-021 的最低商品输入之一，但本 Decision **不授权**将商品图片二进制、图片 URL 或图片识别猜测发送给 DeepSeek；商品图片仍主要用于后续数字人手持商品视频生产；未来如需视觉模型读取商品图片，必须形成新的能力与数据边界决定。本 Decision 只确认直接调用 DeepSeek 官方 API，不声称已确认数据驻留地区、数据保留期限、合规认证、SLA、企业专属隔离或任何官方文档未明确支持的隐私保证。
  18. **DeepSeek 输出只能成为 CopyVariant 草稿**，不得直接成为 approved 文案。
  19. **D-021 和 D-022 的所有事实、安全、质检和人工确认规则继续适用**：所有 DeepSeek 输出仍必须只基于已确认事实、作为 CopyVariant 草稿、经过质检、经过人工确认；只有 approved CopyVariant 才能进入 VideoPlan。D-023 不得削弱或覆盖 D-020、D-021、D-022。
  20. **Q-019 由 D-023 正式解决并关闭。**
- **影响（Consequences）**：
  - MVP 的文案生成 Provider、默认模型和运行模式已经确定；
  - 企业用户无需配置 LLM Key；
  - SaaS 平台承担 DeepSeek 凭证和调用成本管理；
  - Provider 依赖被限制在 LLM Provider Adapter；业务领域保持 Provider-neutral；
  - 前端不显示 API Key；前端不提供 Provider 或模型选择器；
  - DeepSeek 返回结果不能直接成为 approved 文案；JSON Output 后仍必须执行服务端校验；
  - 输出形态失败最多一次同模型受控重试；重试仍失败则任务失败、不保存不完整 CopyVariant、由用户手动重试；
  - 不存在自动 V4-Pro fallback；不存在自动 Provider fallback；不存在第三方中转数据边界；
  - API Key 只存在于服务端，不进入 Git、Markdown、浏览器、业务表、日志、错误信息、分析事件、截图、PR body、测试 fixture 或生成文案结果；
  - Q-019 从 Phase 0 剩余门禁中移除并关闭；Q-004 继续保持 OPEN；
  - 本 Decision 不代表 DeepSeek 已经完成代码接入；
  - 本 Decision 不定义 timeout、并发、限流和完整错误重试策略；
  - 本 Decision 不确定腾讯云的具体 Secret 产品；Q-021 仍负责云基础设施选择；
  - Phase 0 仍未全部完成。
- **被否决方案**：
  - 使用第三方 DeepSeek API 中转 Key；
  - 使用模型聚合平台作为默认 Provider；
  - 使用 `deepseek-chat` 或 `deepseek-reasoner` 旧别名；
  - 默认使用 `deepseek-v4-pro`；
  - 依赖思考模式默认值而不显式关闭；
  - 默认启用思考模式生成批量文案；
  - 业务代码直接调用 DeepSeek SDK；
  - 将 API Key 放入浏览器；
  - 让企业用户在 MVP 自带 Key；
  - 非法 JSON 仍保存为文案；
  - 无限自动重试；
  - 自动从 Flash 切换到 Pro；
  - 自动从 DeepSeek 切换到其他 Provider；
  - 把商品图片自动发送给 DeepSeek；
  - 将未经确认的商品信息放入生成请求。
- **可重新评估条件**：只有在以下情况发生时重新评估：
  - DeepSeek 官方停止提供 `deepseek-v4-flash`；
  - 官方模型名、接口或思考模式合同发生不兼容变化；
  - 质量评测证明 Flash 无法满足核心文案要求；
  - 延迟、成本或可用性发生重大变化；
  - 企业提出强制 BYOK；
  - 企业合规要求不允许当前数据边界；
  - 产品需要多 Provider 容灾；
  - 需要支持视觉输入；
  - 需要平台专属复杂推理；
  - V4-Pro 或其他模型经过正式质量和成本评测；
  - Q-021 基础设施决定提出新的 Secret 或网络边界要求。

## D-024 MVP 管理员预创建账号、邮箱密码登录与单组织进入

- **日期**：2026-08-04
- **背景（Context）**：产品是企业内部平台，不面向公众（D-014）；第一版是单企业、单组织的 Cloud-first Web MVP（D-014 / D-015）；Phase 1 需要完成登录和组织身份边界，但第一版登录方式仍未决定（Q-022）。Q-022 之前列出的候选包括：管理员预创建账号、邀请链接、企业邮箱账号、手机号、企业微信登录；需要选择依赖最少、边界清晰且适合企业内部 MVP 的方案。登录身份不能替代 OrganizationMember 授权关系：User 表达登录身份，OrganizationMember 表达组织访问资格，两者必须分离建模。低保真登录页和成员管理流程依赖该决定。与已有 Decision 的关系：D-014 确定企业内部用户与单企业/单组织 MVP 并保留 Organization 边界；D-015 确定 Cloud-first Web 产品且 Phase 1 提供云端登录；D-016 确定不建设支付、套餐或公众商业注册体系，但企业权限与治理仍重要。D-024 负责确定上述边界下的第一版具体登录方式，不得削弱或覆盖 D-014、D-015、D-016。
- **决策**：
  1. **MVP 不开放公众注册**：不提供公众自助注册、用户自行创建企业或 Organization、通过邮箱域名自动加入企业、公开邀请注册、组织选择器或多组织切换器。
  2. **第一版采用管理员预创建账号。**
  3. **用户使用管理员录入的工作邮箱和密码登录。**
  4. **工作邮箱是登录标识，但不开放基于邮箱域名的自助注册或自动入组。**成员身份的可信来源是管理员预创建账号并建立 OrganizationMember 关系；不得将「使用企业邮箱」解释为只要具有同一邮箱域名任何人都可以自行注册或加入企业。MVP 不要求邮箱验证码、邮箱域名验证、邮件激活或邮箱域名自助加入组织。
  5. **初始 Owner/Admin 由部署初始化流程创建。**本 Decision 只确定产品行为，不固定具体初始化实现（允许未来实现为部署初始化命令、一次性 bootstrap 脚本、管理后台初始化流程或其他经过安全评审的部署方式）；本轮不创建初始化代码、真实账号、真实密码，不固化 CLI 命令或数据库 seed 格式。初始 Owner/Admin 具备最小的成员管理权限；MVP 至少区分具有成员管理权限的 Owner/Admin 与普通企业成员；完整 RBAC、细粒度权限矩阵和企业治理属于后续范围。
  6. **管理员创建成员时，同时建立该用户与唯一 Organization 的 OrganizationMember 关系。**管理员可以：输入成员工作邮箱、创建成员登录账号、将用户关联到唯一 Organization、建立 OrganizationMember、查看成员的当前生命周期状态、为成员重置临时密码、停用成员。管理员预创建成员时，不得要求成员先完成公开注册。普通成员不得预创建其他成员，除非后续权限决策明确授予其成员管理权限。
  7. **新成员使用临时密码首次登录。**临时密码由系统生成或分配，通过管理员认可的安全渠道交付给成员。本 Decision 不固定临时密码具体生成算法、有效期、交付工具、密码长度、密码复杂度、密码哈希算法或认证框架。
  8. **首次登录必须修改临时密码。**
  9. **完成密码修改前不得访问组织业务数据**；修改完成后，账号进入正常可用状态。临时密码只能用于账号激活流程。
  10. **登录后自动进入唯一 Organization。**正常登录流程：输入工作邮箱和密码 → 验证登录身份 → 检查账号和成员状态 → 找到唯一有效 OrganizationMember → 自动进入唯一 Organization → 进入首页或项目列表。
  11. **MVP 不显示组织创建、组织选择或多组织切换**：登录后不展示创建组织、选择组织、切换工作区、加入其他企业或多组织列表。
  12. **零个有效组织成员关系时拒绝进入系统**：显示可理解的账号配置提示，引导用户联系管理员。
  13. **多个有效组织成员关系在 MVP 中属于配置错误**：不得静默选择任意组织，不得临时增加组织选择器；应视为违反 MVP 单组织约束的配置错误，拒绝进入业务系统并要求管理员处理。本 Decision 不固定具体错误文案。
  14. **身份与成员关系分离**：User 负责登录身份；OrganizationMember 负责用户对 Organization 的成员关系和访问资格；Role 负责成员权限；业务数据继续归属 Organization 或 Project；不得因为第一版只有一个组织就删除 Organization 边界。授权判断必须基于有效的 OrganizationMember，不能只因为 User 登录成功就允许访问组织数据。
  15. **MVP 生命周期至少包含：`pending_activation`、`active`、`disabled`**。`pending_activation`：管理员已预创建账号，但成员尚未完成首次密码修改；`active`：成员已完成激活，可以正常登录和访问组织；`disabled`：成员已被管理员停用，不得继续访问组织数据。本轮不固化数据库字段名、状态字段位于 User 还是 OrganizationMember、ORM 枚举、API 枚举或 Migration。
  16. **disabled 成员不得继续访问组织数据**：新登录必须失败；已有访问不得继续获得组织数据；不得只隐藏页面而继续允许 API 访问。具体会话撤销、Token 失效和请求级检查方式属于后续实现规格，本轮不固定技术方案。
  17. **忘记密码暂由管理员重置为新的临时密码**：用户联系管理员 → 管理员执行密码重置 → 系统生成新的临时密码 → 用户使用临时密码登录 → 强制设置新密码。管理员执行重置后不得查看用户原密码、不得恢复用户原密码，只能产生新的临时密码；用户必须再次完成密码修改后才能正常进入组织。本 Decision 不固定临时密码失效时间、是否立即失效所有已有 Session、密码历史规则、错误次数锁定参数或登录限流参数；这些属于安全实现规格。
  18. **MVP 不提供自助邮件密码找回**：不提供自助邮件找回密码、邮件重置链接、短信验证码找回密码或客服人工身份验证系统。
  19. **MVP 不接入其他登录方式**：不接入手机号登录、短信验证码、企业微信登录、企业微信成员同步、企业 SSO、SAML、OIDC 企业身份源、社交账号登录、魔法链接登录、公开邀请链接注册、自助邮箱注册、自助密码找回或 MFA。这些能力可以在后续重新评估，但不得在低保真原型中显示为已经支持。
  20. **管理员不得查看成员当前密码。**
  21. **密码不得以明文长期保存**；真实密码不得进入日志、错误信息、Markdown、Git、截图或 PR。
  22. **完整 RBAC、MFA、SSO、自助邀请和多组织能力后置。**
  23. **Q-022 由 D-024 正式解决并关闭。**
- **影响（Consequences）**：
  - Phase 1 登录页只需要邮箱和密码；
  - 登录页不显示注册入口；
  - 登录页不显示手机号或企业微信登录；
  - 忘记密码区域提示联系管理员；
  - 首次登录需要独立的设置新密码步骤；
  - 登录后直接进入首页或项目列表；
  - 不建设组织选择页；
  - 需要基础成员管理入口；
  - 管理员可以预创建成员、查看生命周期状态、重置临时密码和停用成员；
  - User 与 OrganizationMember 继续分离；
  - 完整 RBAC 不进入 Phase 1；
  - 邮件服务、短信服务和企业微信 OAuth 不成为 Phase 1 前置依赖；
  - Q-022 从 Phase 0 剩余门禁中移除并关闭；Q-021 继续保持 OPEN；
  - 低保真页面结构仍需制作；
  - Phase 0 仍未全部完成；
  - 本 Decision 不代表认证功能已经实现；
  - D-024 不得削弱或覆盖 D-014、D-015、D-016。
- **被否决方案**：
  - 公众自助注册；
  - 仅凭企业邮箱域名自动加入组织；
  - 公开邀请链接作为第一版主要开户方式；
  - 手机号和短信验证码登录；
  - 企业微信登录；
  - 企业 SSO；
  - 用户自行创建 Organization；
  - 登录后要求选择唯一组织；
  - 多组织切换；
  - 管理员设置并长期掌握成员永久密码；
  - 明文保存密码；
  - MVP 首先建设自助邮件找回密码。
- **可重新评估条件**：只有在以下情况发生时重新评估：
  - 企业明确要求员工自助入组；
  - 邮件发送能力已经稳定建设；
  - 企业要求企业微信成员同步；
  - 企业要求 SSO、SAML 或 OIDC；
  - 产品开始支持多组织；
  - 产品开始面向外部客户；
  - 成员规模使管理员预创建成本不可接受；
  - 安全评审要求 MFA；
  - Phase 4 企业治理需要更完整身份生命周期；
  - 企业需要域名验证、自动入组或 SCIM。

## D-025 MVP 文案质检、人工审核与批准门禁

- **日期**：2026-08-05
- **背景（Context）**：D-021 已确定所有 AI 文案必须基于用户确认的商品事实，文案先作为草稿，经质检和人工确认后才能进入 VideoPlan；D-022 已确定 ContentBrief 为可选输入，自定义表达均需经过相同事实安全与文案质检；D-023 已确定使用 DeepSeek JSON Output 返回结构化生成结果，服务端执行 JSON 解析与业务 Schema 校验，输出形态失败最多一次同模型受控重试。Q-004 仍缺少：质检规则来源与权威优先级、各类风险的阻断或提醒等级、LLM 语义质检的职责边界、人工审核角色与越权边界、品类规则策略、质检失败后的修改和重新审核流程。D-025 负责关闭上述缺口，并明确「自动质检通过」与「人工批准」是两个独立状态。与已有 Decision 的关系：D-025 不得削弱或覆盖 D-021 商品事实门禁、D-022 ContentBrief 可选输入、D-023 生成运行契约；D-025 只在产品规格层固化质检与批准门禁，不代表任何质检功能已经开发，也不固定数据库、ORM、API、Migration 或前端组件实现。
- **决策**：
  1. **规则来源与权威优先级**：文案质检采用分层规则体系，不采用可相互抵消的百分比权重。权威顺序为：`已确认商品事实 → 平台强制规则 → 企业/品牌规则 → LLM 语义质检 → 人工业务确认`。语义：已确认商品事实是事实性声明的证据来源；平台强制规则不可被企业、品牌、LLM、人工或管理员覆盖；企业和品牌规则可以增加或收紧要求，但不能降低平台门禁；LLM 负责语义发现、解释和修改建议，不拥有最终放行权；人工只能在上层边界内完成业务确认；人工点击不能把未经确认的内容变成商品事实；管理权限不等于内容安全规则豁免权。
  2. **自动质检结果**：MVP 自动质检结果为 `invalid` / `blocked` / `needs_review` / `passed`。`invalid`：质检过程或输入版本无效，未得到可信的正式结果；`blocked`：存在不可绕过的事实、平台、法务或高风险规则失败；`needs_review`：存在可由有权限审核人逐项判断的语义或质量 finding；`passed`：不存在阻断项，也不存在尚未处理的 review finding。**必须明确 `passed ≠ approved`**：`passed` 是自动质检结果，`approved` 是人工批准结果；`invalid` 和 `blocked` 不能被人工直接覆盖；硬阻断不能被其他维度得分抵消；不采用「事实安全低分 + 表达自然高分 = 平均后放行」的模式。结果聚合顺序为：`必要质检步骤未完成 → invalid`；`否则存在任一 hard block → blocked`；`否则存在任一未处理 review finding → needs_review`；`否则 → passed`。
  3. **MVP 确定性规则覆盖**：至少在产品规格层覆盖以下规则组：(1) 质检输入与版本完整性；(2) 模板、Prompt、调试内容和敏感信息泄漏；(3) 价格、容量、尺寸、重量、数量、日期、折扣、性能参数等数值事实核对；(4) 医疗、健康、功效、认证、排名、销量、奖项、保证、价格、优惠和库存等高风险事实门禁；(5) 平台、企业和品牌禁用词；(6) 企业或品牌必需表达；(7) 核心卖点最低覆盖；(8) 重复、乱码、异常标点和异常格式；(9) 文案长度、技术上限和期望口播长度偏差；(10) 文案、商品事实、ContentBrief、品类和规则版本绑定。每条 finding 至少具有概念级信息：`rule_id`、`rule_version`、`rule_source`、`rule_name`、`scope`、`severity`、`matched_excerpt`、`evidence`、`reason`、`remediation`。可以保留质量维度或辅助分数，但分数不是放行依据、分数不能覆盖 hard block、用户必须能看到命中片段/原因/证据/修复建议、不得只展示一个总分。
  4. **LLM 语义质检职责**：LLM 被定义为受约束的语义审查器，不是商品事实来源、规则制定者、最终审批者或硬规则覆盖者。LLM 检查范围至少包括：事实支持关系、未经确认的事实外延、隐含夸张或确定性承诺、核心卖点是否真正表达、自然口播与句子可读性、语义重复和信息密度、品牌风格与 ContentBrief 一致性、收尾方式、前后矛盾、隐含销量/评价/排名/竞品/功效暗示。LLM finding 概念级输出至少包括：`dimension`、`finding_type`、`severity_suggestion`、`matched_excerpt`、`claim_summary`、`evidence_refs`、`reason`、`confidence`、`remediation`。要求：服务端正式规则决定最终 severity；明确矛盾或无证据高风险事实进入 `blocked`；无法确定的语义风险进入 `needs_review`；`confidence` 只能用于排序或排查，不能用于忽略事实风险；不保存或展示模型内部思维链；只保留简洁、可审计的理由和证据。LLM 质检技术失败包括：空内容、非法 JSON、截断、Schema 不符、必要维度缺失、Provider 超时或不可用；最多进行一次相同配置的受控重试，重试只处理技术或输出结构失败，不能因为第一次发现内容问题而自动重试以寻求更宽松的结果；重试后仍失败则 `quality result = invalid`。
  5. **人工审核能力与越权边界**：MVP 在产品概念层区分四类业务能力：文案编辑能力、文案审核能力、商品事实管理能力、品牌规则管理能力。本轮不固定完整 RBAC、数据库权限表或 API permission enum。文案审核人可以：审核 `passed` 文案、查看 finding/证据/修改建议、对 `needs_review` finding 逐项处理、要求修改、驳回、对允许人工判断的问题记录接受理由后批准。任何人（含 Owner/Admin）均不能：覆盖 `invalid`、覆盖 `blocked`、将未经确认的事实直接批准、修改正式质检结果、跳过重新质检、让已失效的 approval 继续有效。人工审核状态独立于自动质检状态，概念级状态为 `not_submitted` / `pending` / `approved` / `changes_requested` / `revoked`。`needs_review` finding 不能通过「忽略全部」批量处理，必须逐项记录 `finding_id`、`reviewer_user_id`、`accepted_at`、`acceptance_reason`。MVP 允许拥有审核能力的用户审核自己生成或编辑的文案，但必须记录 `author_user_id`、`reviewer_user_id`、`self_review`；强制双人审核作为后续组织策略，不是 MVP 强制门禁。
  6. **品类规则**：MVP 支持版本化的品类质检规则档案，但不建设庞大、固定的电商类目树。规则组合为：`平台通用规则 + 企业/品牌规则 + 主要品类规则档案 + 当前已确认商品事实`。约束：每件商品 MVP 使用一个主要品类规则档案；未配置品类时使用通用规则；品类不是文案生成的新必填条件；品类规则只能加严，不能削弱平台门禁或商品事实门禁；选择或缺少 `general` 不能绕过医疗、健康、安全、认证、销量、排名等通用高风险检查；不在本 Decision 固化完整品类枚举；不声明已经拥有完整法律法规合规库；品类规则变化后旧质检和旧 approval 失效。品类档案可在概念层包含：高风险声明类型、需要证据的事实类型、品类禁用词、品类必需提示、确定性检查、LLM 重点审查维度、默认严重等级、修改建议。
  7. **修改、重检与批准流程**：文案正文不得原地覆盖历史。以下操作创建新的 CopyVariant / CopyVersion：用户修改正文、AI 局部改写、AI 全文改写、重新生成、切换另一候选文案、表达风格/种草角度/收尾等变化后生成新内容。AI 改写流程为：`选择 finding → 请求 AI 改写 → 创建新文案版本 → 用户查看 → 完整重新质检 → 再次人工审核`；正式质检不能只检查修改句子，正文变化后必须运行完整质检。以下变化即使文案正文不变也使旧质检和旧批准失效：商品事实新增/修改/删除、核心卖点变化、ContentBrief 中影响表达的内容变化、主要品类变化、平台规则版本变化、企业或品牌规则版本变化、品类规则版本变化。失效语义为：`旧 QualityCheck → superseded/历史记录`，`旧 Approval → revoked`，`重新完整质检`，`重新人工审核`；正式质检记录和正式审核记录不可原地篡改。进入 VideoPlan 时必须再次确认：CopyVariant 是当前有效版本、商品事实版本仍有效、ContentBrief 版本仍有效、品类与规则版本仍有效、最新正式质检有效、不存在 invalid 或 blocked、needs_review finding 已逐项处理、存在当前有效 approved；任一条件失败时拒绝进入或执行 VideoPlan。已创建但尚未执行的 VideoPlan 若引用的文案 approval 失效，应进入不可执行状态，要求重新选择已批准文案或重新审核；历史已完成视频不自动删除，但必须保留其所引用的历史文案和审批记录。
  8. **批量操作边界**：MVP 可以支持：批量发起质检、批量查看状态、批量重新检查 `invalid`、批量提交 `passed` 文案审核、批量批准不存在任何提醒且版本完全有效的 `passed` 文案。MVP 不允许：批量忽略 `needs_review`、批量覆盖 `blocked`、批量批准未经确认的事实、批量沿用失效 approval、一键接受所有 finding。
  9. **Q-004 由 D-025 正式解决并关闭。** D-021 商品事实门禁继续有效；D-022 ContentBrief 可选输入继续有效；D-023 DeepSeek 生成运行契约继续有效。D-025 不代表任何质检功能已经开发；D-025 不固定数据库、ORM、API、Migration 或前端组件实现。
- **影响（Consequences）**：
  - 自动质检状态与人工审核状态在概念层分离，`passed ≠ approved`；
  - `blocked` 与 `invalid` 不可被人工或管理员覆盖；
  - 文案、事实、ContentBrief、品类或规则版本变化后必须完整重新质检并重新审核；
  - 只有当前有效 `approved` 的 CopyVariant 才能进入 VideoPlan；
  - finding 必须可解释（命中片段、原因、证据、修复建议），不得只展示总分；
  - Phase 1 SAAS-001B 范围增加分层质检与人工批准门禁；
  - 完整 RBAC、双人审核强制、品类完整枚举和法律法规合规库不进入 MVP；
  - 本 Decision 不代表 CopyQualityService、规则引擎或任何质检 UI 已经实现；
  - D-025 不得削弱或覆盖 D-021、D-022、D-023。
- **被否决方案**：
  - 百分比加权后抵消事实阻断（「事实安全低分 + 表达自然高分 = 平均后放行」）；
  - 只使用禁用词列表作为质检；
  - 只使用 LLM 质检，由 LLM 直接放行；
  - 只显示总分，不暴露具体 finding；
  - 自动质检 `passed` 后直接进入 VideoPlan，无人工批准；
  - 人工或管理员覆盖 `blocked`；
  - 批量忽略所有 `needs_review` 提醒；
  - 原地覆盖文案正文和质检/审核记录；
  - 品类必须先建设完整类目树才能质检；
  - 将 LLM 判断当作商品事实；
  - 修改文案后沿用旧 approval 或只检查改动句子。
- **可重新评估条件**：只有在以下情况发生时重新评估：
  - 企业要求强制双人审核或更细粒度审核权限；
  - 出现需要完整法律法规合规库的品类；
  - 品类规模使「单一主要品类档案」不再适用；
  - 需要更复杂的多维度评分与权重策略（仍不得抵消事实硬阻断）；
  - LLM Provider 能力变化需要调整语义质检维度；
  - Phase 4 企业治理要求完整 RBAC 与审计。

## D-026 MVP 云基础设施与两阶段部署策略

- **日期**：2026-08-05
- **背景（Context）**：D-015 已确定 Cloud-first Web 产品（默认部署腾讯云），但具体云基础设施选型仍为开放问题 Q-021（数据库、对象存储、队列、SecretStore、日志监控、备份、环境划分、成本预算均未决定）；D-019/D-020/D-023 已确定文案为 SaaS 自有能力、DeepSeek 为平台默认 LLM Provider、Secret 遵守安全底线；D-014/D-015 已确定企业内部、单企业单组织、Cloud-first。D-026 负责关闭 Q-021：在产品规格层固化云基础设施方向、区分个人验证环境与企业正式生产环境、明确计算/数据库/存储/任务/Secret/观测/备份边界。详细 Specification 见 [CLOUD_INFRASTRUCTURE.md](CLOUD_INFRASTRUCTURE.md)。D-026 不代表任何云资源已经部署，也不固定企业最终实例规格、预算、域名或已有资源；Q-018（飞影 API Token 保管与调用位置）不在本 Decision 范围，保持开放。
- **决策**：
  1. **Cloud Control Plane 与 Local Agent 分离**：腾讯云负责云端控制面；Local Agent 负责需要本地文件、浏览器登录态、长时间自动化和人工接管的执行；领域层不绑定腾讯云，Database/ObjectStorage/TaskQueue/SecretStore/LogSink 等通过基础设施抽象接入。
  2. **模块化单体 + API/Worker 双部署单元**：MVP 使用模块化单体而非微服务系统；API 与异步 Worker 是两个独立部署单元；不建设 Kubernetes、服务网格或复杂分布式基础设施；长时间 Playwright、视频编码和本地大模型不得运行在 Web/API 请求生命周期。
  3. **个人 2C4G 验证环境**：使用 owner 已有 2 核 4 GB 测试服务器 + 单机 Docker Compose，单 API 实例、单 Worker（并发 1）、PostgreSQL、反向代理；只验证功能闭环、持久化、任务恢复、幂等与 Local Agent 协议；不承担生产 SLA、高可用、并发容量或灾备验收；测试通过不代表生产规格达标。
  4. **企业正式环境以腾讯云广州为主地域**（`ap-guangzhou`）：生产资源尽量同地域、同 VPC；PostgreSQL 不开放公网；staging 与 production 完全隔离。
  5. **CloudBase Run API + Worker**：`hifly-web-api` 与 `hifly-async-worker` 两个独立容器服务；API 处理同步请求/身份/事务/任务创建/状态查询，创建 AsyncJob 后返回 job_id；Worker 处理文案生成、LLM 语义质检、批量任务与状态推进；核心业务不依赖 CloudBase 专有身份/数据库/领域 SDK；TKE 不进入 MVP；Lighthouse/CVM 为开发或回退方案；CloudBase Run 具体限制需部署前验证。
  6. **TencentDB for PostgreSQL 为唯一权威关系型业务数据库**：production 使用托管高可用主实例；核心业务字段用关系型列/外键/约束，仅弹性 finding/规则快照/证据引用/Provider 摘要存 JSONB；Schema 变更走版本化 Migration；禁止启动时自动执行不可逆生产 Migration；最小权限账号、受限连接池；MVP 不建分库分表/读写分离/跨地域双活。
  7. **Tencent Cloud COS 对象存储**：至少划分 sensitive（数字人原始照片/视频、声音克隆源文件、授权证明等可识别个人身份源素材）与 content（商品图片、业务附件、中间产物、正式输出、缩略图、失败证据）两类私有桶；默认私有读写，浏览器经业务 API 获取短时最小权限上传/下载授权；不保存永久公开 URL，永久 SecretId/SecretKey 不进入浏览器；API 必须在前端报告完成后重新核验对象；对象键用内部 ID；加密敏感桶优先 SSE-KMS、内容桶默认 SSE-COS，SSE-KMS 地域/费用需部署前验证。
  8. **PostgreSQL AsyncJob / Transactional Outbox**：MVP 不单独采购消息队列；API 在同一数据库事务提交业务变更与 AsyncJob；Worker 用 `FOR UPDATE SKIP LOCKED` 领取，租约+心跳+过期恢复；交付语义 at-least-once，不宣称 exactly-once；每种任务用稳定 idempotency key；只对瞬时技术故障有限重试，blocked/事实不足/权限失败/Schema 错误不自动重试（D-023 LLM 输出形态失败仍最多一次同配置受控重试）；Worker 初始常驻单副本（min=max=1）。
  9. **SSM + KMS + CAM + STS**：SSM 保存数据库密码/DeepSeek API Key/签名密钥等服务端凭据；KMS 保护密钥；CAM 最小权限；STS 浏览器 COS 直传/下载短期凭证；API/Worker/部署/运维身份分离；Secret 不进 Git/镜像/前端/数据库/日志；日志只记 secret_name/secret_version/access_result；CloudBase Run 服务角色访问 SSM/COS/KMS 需部署前验证；凭据支持版本化/轮换/吊销/审计。
  10. **CLS / APM / CloudAudit / PostgreSQL AuditEvent**：应用日志→CLS（结构化 JSON，request_id/trace_id/job_id 关联）；云资源指标→腾讯云可观测平台；调用链→APM/OpenTelemetry；业务审计→PostgreSQL AuditEvent；账号资源操作→CloudAudit；不记录密码/Token/Cookie/完整预签名 URL/完整 Prompt/Response/文案正文/原始照片/声音/授权材料；保留期可在预算核算时调整但不能删业务审计降费。
  11. **备份与恢复**：数据库 RPO≤30 分钟、RTO≤4 小时、应用发布故障 30 分钟内回滚（产品目标，非已取得 SLA，需上线前演练验证）；PostgreSQL 每日全量+日志备份+PITR，常规 30 天、月度 12 个月，恢复优先克隆到新实例验证后切换；COS 开版本控制，MVP 暂不默认跨地域复制；首次上线前及每季度做恢复演练，禁止把生产敏感数据复制到普通 staging。
  12. **正式月度预算 Pending Evidence**：个人阶段只用已有 2C4G；企业生产规格需根据真实负载和既有资源确定；采购前必须盘点企业已有腾讯云资源并判断是否位于广州、是否属于其他项目、能否隔离 staging/production、能否用独立数据库凭据、是否有未知历史配置、是否满足安全与备案要求。
  13. **Q-018 保持开放**：采用 SSM 不等于授权上传 Hifly Token；Q-018 决定前 Hifly Token 不进入既定云端 Secret 清单、不默认进入 CloudBase Run、不从 Local Agent 迁移至云端。
  14. **不把产品主链路绑定 Windows 或低代码 RPA**：Playwright 仍是核心浏览器自动化路径；影刀（Yingdao）仍是可选 Adapter，需独立 Evidence，不在 D-026 做最终工具迁移决策。
  15. **Q-021 由 D-026 正式解决并关闭。** D-026 只固化架构方向、环境边界和后续部署验收要求，不代表任何云资源已经部署，也不固定数据库、ORM、API、Migration 或前端组件实现。
- **影响（Consequences）**：
  - 个人验证在 2C4G Docker Compose 上进行，企业正式生产在广州按 D-026 架构落地；
  - 模块化单体 + CloudBase Run API/Worker 双部署单元成为 MVP 计算基线；
  - PostgreSQL 成为唯一权威关系型业务数据库，AsyncJob/Outbox 成为 MVP 任务机制，不采购 RabbitMQ；
  - COS 承担大文件与媒体，PostgreSQL 不存大文件；
  - Q-021 从 Phase 0 剩余门禁中移除并关闭；
  - 企业正式预算、最终实例规格、域名、固定出口 IP、跨地域复制、RabbitMQ 升级时机保持 Pending Evidence / 待决；
  - Q-018 继续保持 OPEN；Hifly Token 不默认进入云端；
  - 详细 Specification 在 [CLOUD_INFRASTRUCTURE.md](CLOUD_INFRASTRUCTURE.md)；
  - 下一步进入低保真页面结构（本 Decision 不制作低保真）；
  - 本 Decision 不代表云资源已经部署。
- **被否决或暂缓方案**：
  - 个人阶段提前购买整套企业生产基础设施；
  - 将 2C4G 直接作为企业正式生产规格；
  - MVP 使用 Kubernetes；
  - MVP 拆分大量微服务（auth-service/product-service/copy-service 等）；
  - MVP 一开始采购 RabbitMQ；
  - 把大文件保存进 PostgreSQL；
  - 将长期 Secret 暴露给浏览器；
  - 默认把 Hifly Token 上传云端；
  - 在 Cloud Web 请求进程运行长 Playwright；
  - 将正式预算在缺乏企业资源证据时拍脑袋固定；
  - 将本 Decision 描述为已完成部署。
- **可重新评估条件**：只有在以下情况发生时重新评估：
  - 企业已有腾讯云资源盘点结果要求调整主地域或计算服务；
  - CloudBase Run 部署前验证发现不支持服务角色访问 SSM/COS/KMS，或限制不可接受；
  - Worker 消费者数量或 PostgreSQL 轮询压力达到 RabbitMQ 升级条件；
  - 企业容量/SLA/风险要求提高，需要跨地域复制或更高规格；
  - Q-018 决定影响云端 Secret 清单与执行位置；
  - 企业安全/备案/合规要求变化。

## D-027 MVP 低保真页面结构与交互边界

- **日期**：2026-08-05
- **状态**：Confirmed
- **背景（Context）**：D-014/D-015 已确定企业内部 Cloud-first MVP；D-017 已确定垂直切片两层拆分（Vertical Slice A / B）；D-021/D-022/D-023 已确定商品事实门禁、可选 ContentBrief 与 DeepSeek 生成契约；D-024 已确定管理员预创建账号登录；D-025 已确定文案质检与人工批准门禁；D-026 已确定云基础设施与两阶段部署。Phase 0 仍缺少低保真页面结构与交互边界（Phase 0 门禁中的「低保真页面结构」项未完成），导致 Vertical Slice A 缺少统一的页面、状态、版本、失效、历史保留与业务/技术诊断边界产品依据。D-027 在产品规格层固化 Phase 1 全局信息架构、项目五阶段工作台、Vertical Slice A 完整页面流、状态/版本/失效/历史保留规则、业务界面与技术诊断边界、Phase 1/Phase 2 验收分界。详细 Specification 见 [LOW_FIDELITY_PAGE_STRUCTURE.md](LOW_FIDELITY_PAGE_STRUCTURE.md)。D-027 是产品结构决策，**不代表任何页面、组件或 API 已经实现**，不解决 Q-018，不宣称 HIFLY-001 Evidence 已完成，不改写已有历史决策。
- **决策**：
  1. **Phase 1 一级导航固定为 6 项**：首页、项目、素材中心、生产任务、作品库、设置；不展示组织切换、账单、充值或尚未实现的入口；不创建模板中心、发布管理、数据分析、声音中心、背景与场景、模型配置、Provider 配置、套餐与账单等空页面。
  2. **项目统一五阶段工作台**：商品与目标、文案与质检、人物与素材、视频方案、生成与交付；阶段顺序固定，不满足上游门禁时下游禁止关键操作，上游变化后下游显示「需要重新确认」。
  3. **Vertical Slice A 范围**：只创建空白项目（不提供模板选择）；人物只允许选择公共数字人或企业已有人物（创建新人物属 Vertical Slice B）；声音/背景/场景不是独立选择字段；Playwright、影刀、Provider 不作为普通运营用户选项；Local Agent 位于「设置」并可从阻断提示进入；Phase 1 结束于可追踪的 ProductionOrder 和绑定该工单的人工交接包；Local Agent 真实领取/执行/自动产物回传属 Phase 2。
  4. **登录/首页/项目入口**：工作邮箱密码登录、首次登录改临时密码、自动进入唯一企业（D-024）；首页为工作入口与待办中心，主操作「创建项目」；项目列表为桌面表格；创建项目只创建空白项目，必填名称，描述/交付日期可选，不在创建时选择模板/平台/受众/视频类型/Provider/人物/声音/背景。
  5. **商品与目标三栏工作台**：商品列表 / 商品事实与内容偏好编辑 / 文案生成准备度；生成门禁沿用 D-021（名称+核验图片+确认卖点）；卖点逐条确认，修改已确认卖点重新变为未确认，不提供全部确认/AI 自动确认/管理员绕过；文案生成异步，用户可离开，不展示模型/Key/Prompt/Provider；上游变化使旧质检失效、旧批准撤销但保留历史。
  6. **文案与质检三栏工作台**：不可变版本列表 / 正文编辑与版本对比 / 自动质检与独立人工审核；界面统一用「文案/文案版本/当前版本/历史版本」，不混用 CopyVariant/CopyVersion；QC 状态 invalid/blocked/needs_review/passed 与人工审核状态 not_submitted/pending/approved/changes_requested/revoked 分离；QC passed ≠ 文案 approved；invalid/blocked 不可人工或管理员绕过；needs_review 逐项处理并填理由，未确认事实不能通过接受提醒绕过；AI 改写创建新版本并完整重检，不继承旧质检/旧批准；MVP 允许本人审核但记录 self_review；仅相关权威输入快照变化才触发失效（展示名称/排序/无关备注不撤销批准）。
  7. **人物与素材**：Vertical Slice A 只选择公共/企业已有人物；人物卡片显示业务可用状态（可用/暂不可用/需要执行环境/授权待确认/能力未验证），不显示 Provider avatar ID/Adapter/Cookie/Playwright/影刀/内部任务编号；只展示已有 Evidence 支持的能力；必须明确「确认人物选择」，不因首项/最近使用/推荐静默提交；更换人物使既有 VideoPlan 需重新确认并保留旧方案与批准历史；Local Agent 离线不影响浏览/选择/设计方案，只影响后续真实自动生产。
  8. **视频方案**：VideoPlan 组合 approved 文案+已确认人物+能力证据支持的配置，页面不执行真实生产也不直接操作飞影/Playwright/影刀；VideoPlan 为不可变版本，固定引用商品/文案/人物/配置快照/创建人时间/预检/审核；预检分上游有效性/方案完整性/生产准备度，Preflight passed ≠ VideoPlan approved；Local Agent 离线属生产准备提醒，不阻止保存/预检/审核/创建 waiting_for_executor 工单；只有当前有效 approved VideoPlan 才能创建 ProductionOrder；上游变化使旧预检失效、旧批准撤销，历史 VideoPlan 与已有 ProductionOrder 快照保留。
  9. **生成与交付一致性口径**：approved VideoPlan → 创建 ProductionOrder → 等待 Local Agent 或生成绑定该工单的人工交接包；人工交接包必须绑定 ProductionOrder，不是另一条业务记录；ProductionOrder 业务状态 draft/ready/waiting_for_executor/claimed/running/requires_action/succeeded/failed/cancelled；ProductionOrder └── ExecutionAttempt 分别建模，重试保留每次 attempt、只对可重试技术故障有限重试、不重复创建等价工单/作品、不通过重试绕过失效方案；waiting_for_executor ≠ failed，requires_action ≠ failed；ProductionOrder succeeded 需产物核验完成（对象存在/类型大小/组织归属/工单追踪 ID）并创建正式 Work 后才标记；返工返回上游创建新版本/新工单/新 Work 并保留新旧关系。
  10. **全局生产任务**：只管理 ProductionOrder/ExecutionAttempt/生产异常/产物回传核验；文案生成/AI 改写/自动质检/素材上传核验等普通 AsyncJob 不进入生产任务主表，在原页面显示状态；ProductionOrder 只能从 approved VideoPlan 创建，不能在全局任务页脱离项目新建；禁止批量强制成功/跳过人工处理/覆盖失败原因/重置运行中/绕过失效方案；Phase 1 可定时轮询+手动刷新，不强制 WebSocket。
  11. **作品库与交付**：界面术语「作品」，领域对象 Work，交付事件 DeliveryRecord，Work ≠ DeliveryRecord；作品库只接收核验通过、来源完整、正式 Work 创建成功的作品，生产中候选文件与核验失败记录留在生产任务；交付状态待检查/可交付/需要返工/已交付，与生产状态独立；作品固定引用 ProductionOrder/ExecutionAttempt/VideoPlan/文案/人物/配置快照；要求返工填原因、返回上游新建版本/工单/Work 并保留新旧关系，不替换旧作品文件；Phase 1 不宣称自动回传已打通，可通过受控人工结果登记创建 Work 但必须绑定现有 ProductionOrder，禁止无来源视频伪造成系统作品。
  12. **素材中心**：是生产输入资产库（与作品库分开）；Phase 1 支持商品图片/普通业务素材/已有人物资产记录，不开放创建数字人/声音克隆/独立音色/背景场景编辑器/模板中心/在线视频编辑/Provider 同步/自动生成；上传走短时授权→对象存储→前端报告→服务端核验→Asset→核验成功才可用；普通上传不处理人物隐私源素材/声音克隆源文件/授权证明；素材版本化引用进商品/人物/VideoPlan/ProductionOrder 快照；历史引用素材不能物理删除，只能停用（停用 ≠ 删除）；敏感素材删除期限由 Q-007/Q-008 管理，不在 D-027 擅自固定。
  13. **设置/成员/Local Agent**：设置区为企业信息/成员/Local Agent/系统信息，不提供 Provider/模型/Key/BYOK/Hifly Token 输入/套餐账单/充值/完整权限矩阵/完整审计中心；成员管理支持列表/创建/查看激活/重置临时密码/停用（停用不删历史）；粗粒度角色管理员/运营/审核人/执行器管理员不代表完整 RBAC；Local Agent 区分连接状态（在线/离线/连接中）与业务可用状态（可接收/正在执行/需要人工处理/配置不完整/版本不兼容），在线 ≠ 业务可用，普通运营只看业务状态，诊断信息脱敏且不显示 Token/Cookie/密码/完整路径/完整签名 URL；普通页面不提供「使用 Playwright/影刀」按钮；**Q-018 仍开放**，不设计 Hifly Token 前端输入/保存表单，不假设 Token 保管位置。
  14. **全局反馈/异常/状态边界**：反馈分信息/提醒/阻断/失败，提醒不弱化硬门禁；阻断说明原因/受影响版本/下一步/返回权威页面入口；保存状态统一（未保存/保存中/已保存/保存失败），保存失败保留输入不清空；异步任务由服务端持久追踪、用户可离开、不用虚假百分比/前端自造成功；禁用按钮解释原因；权限不足用业务语言不暴露 403/RBAC 内部规则；区分删除/停用/撤销，区分真正空状态/筛选无结果/加载失败；技术诊断（trace_id/ExecutionAttempt/脱敏证据）进权限控制区。
  15. **一致性收敛**：人工交接包必须绑定 ProductionOrder；Local Agent 离线只阻止自动执行，不阻止 VideoPlan 编辑/预检/审核/waiting_for_executor 工单/人工交接包；全局生产任务只管视频 ProductionOrder 与 ExecutionAttempt；界面用「作品」、领域用 Work、交付用 DeliveryRecord；文案页面统一用文案/文案版本术语；仅相关权威输入快照变化才触发失效；Phase 1 保留作品库入口与人工结果登记但不宣称自动执行/回传；粗粒度角色不等于完整 RBAC；待办入口是轻量入口；人物能力只展示已有 Evidence 支持状态。
- **影响（Consequences）**：
  - Phase 1 有了固定全局信息架构、五阶段工作台与 Vertical Slice A 完整页面流；
  - 自动检查（QC/Preflight）与人工审核（文案/方案 approved）在界面与状态上严格分离；
  - 版本/失效/撤销/历史保留规则统一，上游变化撤销下游批准但保留历史；
  - 业务界面与技术诊断界面边界明确，技术概念不进入普通业务主流程；
  - Phase 0 门禁「低保真页面结构」可标记为产品决策/specification 完成（不代表已实现）；
  - 下一步可依据本规范拆分 Vertical Slice A 开发 Issue 并进入 Phase 1 开发；
  - Q-018 继续保持 OPEN；HIFLY-001 Evidence 仍未完成；
  - 详细 Specification 在 [LOW_FIDELITY_PAGE_STRUCTURE.md](LOW_FIDELITY_PAGE_STRUCTURE.md)；
  - 本 Decision 不代表任何页面、组件或 API 已经实现。
- **非目标**：不实现功能、不固定品牌视觉/组件库/像素尺寸/前端框架/API 路径/数据库表/精确长度文件限制/密码数值策略/Provider 参数；不解决 Q-018；不宣称 HIFLY-001 Evidence 已完成；不改写已有历史决策；不新增未经负责人确认的产品范围。
- **可重新评估条件**：只有在以下情况发生时重新评估：
  - HIFLY-001 Evidence 新增已验证能力，需要开放新的用户可见配置；
  - Phase 2 自动执行闭环落地后，生产任务/作品库交互需要扩展；
  - 企业要求多组织、完整 RBAC、套餐账单或客户门户；
  - 出现需要模板中心、声音中心、背景场景编辑器或在线视频编辑的明确业务需求；
  - Q-018 决定影响 Local Agent / Token 前端边界。
