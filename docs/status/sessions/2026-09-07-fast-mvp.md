# HIFLY FAST-MVP：R0–R2 开发与验证记录

## 2026-09-08 第二次设置政策确认与零积分校准

Owner 最新回复“确认授权”，对应上一轮已经审查的具体设置/比例方案及另一次最多 30 分钟、0 积分校准。声音=播客-女声/普通话，字幕=开启，大小=智能适配，AI 文案=关闭；原片实测并保留，交付片统一无拉伸/无裁切等比补边至严格 9:16。政策见 D-038 新增确认段。真实预算未批准，六条清单仍未整体冻结。

本轮起点 `10046151749fd10c68ad51eb0fb9f9453094c4d0`，工作树干净，远端 main 仍 `2af015a`；PR #285 OPEN/DRAFT，当前头 CI 34170675698 与 Windows stress 34170675697 均 SUCCESS，未合并/部署。实现使用既有 luna-worker，主控独占真实校准并负责文档/独立复核；其他代理不得连接飞影。代码与媒体接口先按现有合同、存储及归档核对，保护历史合同和任务语义。

校准状态：PARTIAL / CLOSED，start=2026-09-08T05:04:25Z，deadline=05:34:25Z。先只读检查本机已打开的飞影页，其会话/作品与 H0 不一致，未调整该页；随后经既有私有隧道启动云端 login-only，会话中 H0 玩偶作品可见。继续核对该云端会话、完整费用和已有作品关联，必要非付费选择临时调整并还原。页面/媒体/遥测与设置写入不是零外部写入；上传、建任务、数字人创建/克隆、生成/再生成、视频下载和外部付费模型上限均为 0。不能为取得回执而点击生成；若无法从授权观察取得证据，记录未知并停止相依部分。


### 第二次校准结果：PARTIAL，窗口已结束

- 窗口 05:04:25Z–05:34:25Z（Asia/Shanghai 13:04:25–13:34:25）；最后设置还原截图写于 05:34:08Z。之后没有继续人工功能检查；05:36:24Z 确认仅剩原先五个服务进程，临时 Chrome、login Node、noVNC 页和 16080 隧道均已关闭。向 login CLI 发送 Enter 的退出尝试发生在窗口后（05:34:39Z 时已发送）。事后核对部署源码发现该确认会先调用 preflight 并导航，因此清理阶段可能额外访问了页面；未采集 HTTP，不能称为零访问或完全在窗口内完成。此偏差保留，后续必须在窗口内完成会导航的确认，或使用明确只关闭上下文的退出方式。Chrome 已关闭，但 login Node 未自行退出，按精确 PID/命令确认后终止；不把关闭画面当作进程退出。
- 本机既有 Chrome Hifly 页的会话/作品与 H0 不一致，未调整该页；后续使用原云端持久 Profile，经私有 noVNC 操作。H0 玩偶作品可见、余额前后观测一致；独立账号 UID 仍未取得，不能将余额或同名作品作为账号唯一标识。此观察不是正式 A/B 样本。
- 当前 `.page-goods .controls-panel .action-footer p.cost-breakdown` 实际包含“图片数字人消耗 350 积分”与“视频创作消耗积分”两部分。信息图标为 `.tooltip-container i.info-icon`，提示文字为“查看积分说明”；本次没有取得视频费率、计费单位/时长或完整单条报价，继续 UNKNOWN。第一轮观察的手持图 150 与本轮图片数字人 350 是不同分项，不能将两者之和当作完整成本。
- 桌面视口中费用区域不可见，临时 DevTools 响应式视口才显示完整 footer；只检查 DOM/显示和提示，没有修改页面 HTML/脚本、调用私有提交接口或点击生成。无新手持图；无因果提交回执，当前还没有可供 resolver 使用的新证据。
- 本轮仅把 AI 文案暂时关闭以查看人工输入模式；显示的既有文本为 46/10000 字符，来源未核验，也未输入/生成新口播。最后已还原 AI/字幕开启、播客-女声/普通话、浏览器 100% 缩放、设备预览缩放 100% 并关闭设备模式和全屏。没有更换人物/商品，没有打开文件上传。
- 实际真实页面/媒体预览/遥测及设置写入发生，HTTP 数量未采集。上传、任务创建、数字人创建/克隆、生成/再生成、视频下载和付费模型调用均 0；余额观测净变化 0，无逐项账单。私有截图/result 在原目录 `outputs/fast-mvp-calibration-20260908-02/`（0700，文件0600），不入 Git；正式 `fast-mvp-runs.jsonl` 仍为空。
- 操作工具限制：本机 CUA 键盘/截图存在遮挡与画面更新延迟；通过浏览器的可聚焦 noVNC canvas 键盘操作及更新截图才确认输入。没有把输入回执当页面成功。后续优先取得同一账号、可直接读取 DOM 的独立浏览器会话，再补报价和关联；不以重复相同环境校准代替真实证据。

### 已批准政策的隔离实现

合同/页面实现初次报告相关测试184/184、静态检查255 JS通过；独立 Review 发现默认 Cloud/Local 的空 verifier 仍在 getter 前阻断，属于真实断点，已交还实现者修复。该结果尚不记 R1 PASS。媒体层复用现有 primary_video/supporting_output 和 A12 保存交付片/原片；权威策略取自经过验证的包内合同，不能用全局配置静默改变旧任务。最终代码/测试/Review/CI 结果在本节继续追加。

Review 未接受无任务预检直接打开浏览器的中间候选，因为它破坏旧 V1/空 verifier 的 runtime 零浏览器保护；主控不把技术解释当 Owner 产品授权。采用既有链路的最小顺序修复：配置/身份/存储检查→只读选择已有候选 order/package→按包合同检查执行能力→实际登录检查→原 claim 事务→任务前置准备/读取→最终付费前只读复核。空队列、旧合同空 verifier 或非法合同不打开浏览器；允许读取已鉴权元数据，坏登录仍零 claim。没有新增队列/state machine，原权威 claim/run 复核不变。真实 runtime 调用链回归与最终 Review 尚待完成，中间候选未合并/部署。

媒体原片/交付片分开记录证据，原片通过 purpose=hifly_original 的 supporting_output 入现有报告并由 A12 强制验证；缺失原片不能登记 Work。主控媒体五文件84/84、0 skip，含本地真实ffmpeg合成媒体转换；日志 `/tmp/hifly-fast-mvp-policy-media-final.log`。这不是业务视频或Provider调用。新增PostgreSQL supporting输出事务回归本机1 skip，等待既有required CI执行，不把skip记通过。A相关10文件初步211/211（`/tmp/hifly-fast-mvp-policy-settings-review.log`）；随后runtime路径和字幕只读修复仍需最终独立复核，计数不相加。


### 最终候选验证与发布边界

新合同的默认设置读取、前置正常设置和最后只读复核已接通；runtime 保留空队列/旧合同无 verifier 的零浏览器保护，新合同登录通过后才领取任务。媒体使用既有 primary/supporting 输出，原片比例与交付片比例分别保存；A12 强制原片完整绑定，缺失原片不能登记 Work。最终实现及新增 PostgreSQL 原片事务回归独立 Review APPROVED。真实回执与完整费率仍未取得，正式 B 仍保持上传/付费前阻断。

本机全量第一次：1422 total / 1404 pass / 1 fail / 0 cancelled / 17 skip，日志 `/tmp/hifly-fast-mvp-policy-full.log`；唯一失败是治理断言仍要求“真实测试未授权”，与已授权零积分校准后的准确文案“真实付费测试未授权”不符。精确修正后治理17/17。第二次：1422 total / 1403 pass / 2 fail / 0 cancelled / 17 skip，日志 `/tmp/hifly-fast-mvp-policy-full-final.log`；Stage2生成后响应等待和Works减少动画即时断言失败。两次失败均保留，不改写为全绿。

测试修复仅等待当前revision的202、当前product/revision草稿响应及实际减少动画样式，保留原断言；Works截图改用临时目录以兼容Linux。两浏览器文件完整4/4通过，生产UI未改。CI复用原identity-postgres job安装FFmpeg，显式传入已安装Chromium路径，并串行执行媒体、默认DOM verifier、Stage2与Works回归；任何skip均使该步骤失败。测试/CI差异独立 Review APPROVED。

主控按新增CI同口径，显式使用Playwright Chromium、清空生产环境、串行运行四文件：27/27 pass、0 fail/cancelled/skip，日志 `/tmp/hifly-fast-mvp-policy-required-local.log`。静态检查255 JS、示例3行与diff check通过。镜像构建、main合并、生产部署、R3六条实验均 NOT_EXECUTED；线上源码与App/Worker revision本轮再次只读核对仍为3e53bff。未来发布需App与Worker同版本并包含FFmpeg，未经阶段授权不激活Worker。回滚保留合同、原片/交付片和所有失败记录，新策略任务不被重写或重提。当前候选提交与CI按 [PR #285 Checks](https://github.com/JettxonHo/hifly-hands-on-product-batch/pull/285/checks) / [Issue #284](https://github.com/JettxonHo/hifly-hands-on-product-batch/issues/284) 推送后核对，不沿用旧头结果。

候选 `860c827` 的 [CI 34219878810](https://github.com/JettxonHo/hifly-hands-on-product-batch/actions/runs/34219878810)：Ubuntu/Windows常规检查通过，新增PostgreSQL原片场景真实执行失败（0 pass / 1 fail / 0 skip），`WORK_VERIFICATION_CONFLICT` 发生于首次缺失原片的请求；之前的并发收据场景复用了同一报告/不同checksum，正在核对fixture隔离，不能削弱生产冲突校验。原日志 `/tmp/hifly-fast-mvp-policy-ci-failed.log` 保留。其后的串行浏览器/媒体步骤未执行。[Windows stress 34219878839](https://github.com/JettxonHo/hifly-hands-on-product-batch/actions/runs/34219878839)通过，不把旧头或其他job通过替代本头完整通过。

该PG失败已定位为fixture干扰：并发收据probe用不同checksum占用了主报告tuple，生产冲突保护按设计拒绝新请求。修复只为probe分配独立succeeded attempt/report/candidate，保留并发冲突断言与主流程的原片失败/recovery/success场景；未改生产规则。独立Review APPROVED，语法/diff检查通过；本机仍因无测试DB为1 skip，新提交必须由CI实际验证。

`23eef2e` 的 [CI 34221131040](https://github.com/JettxonHo/hifly-hands-on-product-batch/actions/runs/34221131040) 已实际通过全部PostgreSQL事务场景、常规Ubuntu/Windows与登录smoke；[Windows stress 34221130994](https://github.com/JettxonHo/hifly-hands-on-product-batch/actions/runs/34221130994)通过。新增串行关键步骤实际27项、24 pass / 3 fail / 0 skip：Works在等待后另取已经脱离DOM的节点样式；Linux两个媒体正向用例因参数兼容失败。原失败日志 `/tmp/hifly-fast-mvp-policy-ci-second-failed.log` 保留。

兼容修复依据 [FFmpeg 6.1源码](https://github.com/FFmpeg/FFmpeg/blob/n6.1/fftools/ffmpeg_opt.c#L1630) 与 [官方文档](https://ffmpeg.org/ffmpeg.html#Video-Options)：旧版autorotate标记要求参数，自动旋转本身默认开启。删除冗余旗标后仍保留实际旋转回归，不禁用旋转或换工具。Works改为同一live DOM快照读取样式再断言，阈值未改；没有继续增加sleep/timeout。主控四文件最终27/27、0 fail/cancelled/skip，日志 `/tmp/hifly-fast-mvp-policy-required-final.log`；独立Review APPROVED。新CI另按提交核验，不把旧失败重标成功。

## 2026-09-08 零积分页面校准授权

Owner 本轮明确回复“确认授权”。窗口：2026-09-07T21:41:11Z 至 22:11:11Z（Asia/Shanghai 为 2026-09-08 05:41:11–06:11:11）。只允许核对 H0 云端账号/会话、设置、当前费用与已有作品标识，以及必要的非付费设置临时调整和还原。页面遥测、Profile 缓存及设置写入单独记录。积分上限 0；上传、生产任务创建、数字人创建/克隆、生成、视频下载、merge/deploy 均不在本授权内。

状态：本次有界校准结束，结论 PARTIAL；该访问窗口已用完，不作为后续访问或六条实验预算。检查在窗口内结束，22:11:11Z 后仅执行退出清理；22:12:28Z 已确认 Chrome 进程消失，随后终止等待标准输入的临时登录进程并关闭 16080 隧道。生产 Worker 未开启 claim。

### 本次实测（仅限观察范围）

| 项目 | 已观察事实 | 尚不能证明 |
| --- | --- | --- |
| 会话 | 同一 H0 云端 Profile 已登录，已有玩偶作品/素材可见；余额前后观测一致，明细仅保留在 Git 外私有记录 | 未独立取得账号 UID；不把余额当六条预算 |
| 声音 | 公共声音按“人物条目 → 风格条目”选择；播客-女声/普通话 → 小镇姑娘/小镇姑娘 活泼成功，刷新后仍为小镇姑娘；已恢复播客-女声/普通话 | 只证明 UI 名称/风格读回，不证明内部 voice_id、服务端 TTS 身份或最终音轨 |
| 声音 DOM | 实际 Elements 中为 `.page-goods .controls-panel .card.auto-voice-box .voice-info .voice-details`，名称 `p.voice-name`，风格 `p.voice-style`；更换声音 button 位于该卡片 | 新自动 getter 尚未在真实页面执行 |
| AI/字幕 | 关闭 AI 后出现一段非 H0 口播的 42 字文本（来源未独立确定）；字幕可关闭。刷新后两者恢复开启，已回到初始状态 | 不能沿用上次的口播或开关状态；必须逐单填写并在最后一次导航后读回 |
| 字幕 DOM | `.card-header` 中 h2 为“字幕”，其 button 有 `type=button`、`role=switch`、`aria-checked=true`，开启时有 `ant-switch-checked` | 未推断未观察的内部参数或最终字幕同步 |
| 手持图 | 外层上传区域为空，但打开弹窗会恢复 H0 已生成手持图；重新编辑显示旧人物/商品两图 | 不能把打开后“已生成”当作新任务结果 |
| 大小/阶段一费用 | 智能适配、超大、大、中、小、超小可见；小 → 智能适配已还原；“立即生成”和“再次生成”均显示 150 积分，均未点击 | 此报价只针对观察到的手持图阶段，不是完整视频报价 |
| 比例/阶段二费用 | 检查的主页面和编辑弹窗未见比例设置；页面文字查找发现 350 一处，但本轮未读取完整阶段二计费上下文 | 完整视频价格、计费时长、硬上限仍 UNKNOWN；不得用历史 613 或历史费率补齐 |
| 作品 | H0 在 goods 显示 2026-09-07 10:17:28，在作品管理显示 10:18；被检查卡片结构为 `.video-common-box`、`.box-main`、`.bottom .box-title/.box-time` | 被检查外层未显示可用任务 ID；没有观察提交响应，因果回执仍 NOT_CAPTURED；不能断言整个站点不存在 ID |

### 外部行为和成本

- 已访问真实 Hifly 页面，发生页面加载、媒体预览和非付费设置变更；声音跨刷新保留，持久位置未检查。页面有事件/遥测脚本，HTTP 请求量与遥测字段未采集，不能报为零外部写入。
- 生成/再次生成/下载按钮点击均为 0；素材上传、创建数字人、创建生产任务、额外模型调用均未执行。页面自动加载/预览已有视频，不等于本次导出或下载新视频文件。
- 余额观测净变化 0；没有逐项账单。期间没有批准或触发付费生成，也没有把空预算视为免费。
- 远程画面有延迟，连续焦点/键盘动作曾漏掉地址首字符，误访问 `ifly.cc/video` 并得到 NXDOMAIN；没有传输账号或素材，随后分开核对地址并回到 `hifly.cc/video`。这属于校准环境问题，不能当 A 组正式耗时或纯 CUA 全链路成功。
- 临时浏览器全屏与缩放已恢复；声音、AI、字幕、智能适配已恢复。登录 CLI 的本机命令未保持可写 stdin，无法发送 Enter；关闭唯一临时 Chrome 标签后，按精确 PID/命令终止本轮登录进程。没有改生产配置或关闭既有服务。
- 私有截图和 `result.json` 位于原目录 `outputs/fast-mvp-calibration-20260908/`，目录 0700、文件 0600，未进入 Git：`voice-subtitle-dom.png`、`retained-handheld-150-points.png`、`handheld-smart-fit-150.png`、`final-works-page.png`。它们不是正式 A/B 运行记录；正式 JSONL 仍为空。

### 对 R1 的直接影响

当前 Cloud verifier 在 `prepareAsset` 的页面导航前执行；本次证实刷新会改变开关，因此早期检查不能代替真正付费前的最后一次检查。优先复用现有页面核心，在最后一次导航/准备后、任何付费动作前读回口播、声音和字幕。声音 UI 证据可补齐，但完整报价、冻结声音/字幕期望以及真实因果回执仍须补足；不得通过宽松的默认 PASS 提前关闭 R1。

窗口结束后仅做已授权的离线修复：复用同一个 `contractFieldVerifier`，从 Cloud `createAsset` 上下文传到既有 Hifly page，保留早期检查，并在最终准备后、`asset_paid_action_pre` 前再次执行。晚期回调只能读取最终准备状态，不能再次导航/重置。晚期失败使用 `pre_paid_gate` 和既有结构化证据，不写付费标记、不点击生成；Review 还修正了此时误报 `unknown_post_submit` 的问题。既有 batch 状态机将该失败持久化为 `failed_pre_submit`，保留证据、清除执行绑定并停止重试，重启后不再误走提交不明的对账流程。没有增加新校验器服务、任务期望默认值、Schema 或真实 DOM getter；Local/legacy 原行为保留。

主控最终 9 文件兼容回归 198/198 pass、0 fail/cancelled/skip（Cloud adapter、batch、executor adapter、snapshot、Cloud service、Local service/system、两份治理测试），日志 `/tmp/hifly-fast-mvp-calibration-final.log`；覆盖早检通过但最终状态改变时零付费标记/点击，以及重启后零再次生成/对账/提交。静态检查 254 文件与 diff check 通过。独立最终 Review APPROVED。实现提交 `a7c8abb`。新代码没有在真实 Hifly 上运行；远端结果在推送后按候选头核对，记录于 [Issue #284](https://github.com/JettxonHo/hifly-hands-on-product-batch/issues/284) 与 [PR #285 Checks](https://github.com/JettxonHo/hifly-hands-on-product-batch/pull/285/checks)，不沿用 7f74bbc 的结果。

远端复核：`9fd0d6c` 的 [CI 34169477252](https://github.com/JettxonHo/hifly-hands-on-product-batch/actions/runs/34169477252) 三项及 [Windows stress 34169477261](https://github.com/JettxonHo/hifly-hands-on-product-batch/actions/runs/34169477261) 均 SUCCESS。随后仅文档提交 `9f2a65e` 的 [CI 34170063302](https://github.com/JettxonHo/hifly-hands-on-product-batch/actions/runs/34170063302) Ubuntu 全量出现 1 个失败（1397 total / 1282 pass / 1 fail / 0 cancelled / 114 环境 skip）：`cloud-executor-deployment.test.js:213` 的 login 启动顺序测试实际记录了两次 `xdpyinfo`，而断言要求一次。Windows 与 identity-postgres 通过，[Windows stress 34170063318](https://github.com/JettxonHo/hifly-hands-on-product-batch/actions/runs/34170063318) 通过。原失败日志保留在 `/tmp/hifly-fast-mvp-final-ubuntu-ci.log`；当时停止直接重跑，先定位启动检查次数差异。

根因是测试替身在启动日志和 ready 标记之间存在调度窗口，而生产 entrypoint 本来就允许有限轮询。初次只交换 marker/log 的候选被主控退回，因为会产生另一种调度竞态。最终只改同一测试文件：显式记录 not_ready/ready，以第二次启动后探测才成功的确定性 fixture 验证轮询；要求 ready 之后才能启动 VNC、noVNC 与 Node，保留 stale lock/socket 和存活 PID 保护，生产脚本未改。主控独立 deployment 与 Cloud adapter 全文件 32/32、0 skip，日志 `/tmp/hifly-fast-mvp-entrypoint-regression.log`，语法/diff check 通过；独立 Review APPROVED。此次计数与前述 198 项重叠，不相加。新提交 CI 按 PR 当前头另行记录。

实验草案据本次观察建议两组使用播客-女声/普通话、智能适配、字幕开启、AI 文案关闭；这些只是未冻结建议，尚未进入正式任务合同，更不是执行或费用授权。P1/P3/人物 B、完整报价与因果回执仍是缺口。

### 待 Owner 决定的最小设置政策（未批准、未实施）

现行 `src/execution-contracts/hifly-hands-on-product-evidence.js` 的 `HIFLY_PRE_PAID_REQUIREMENTS.target_aspect_ratio` 要求在第一次付费前以真实证据证明严格 9:16。本次检查的页面没有提供这一证据。手持图 `record_only` 政策不解除该前置条件；不能把生产合同的目标值复制为已观察值。

建议仅调整这一比例政策：生成前仍记录 9:16 目标、实际可控设置与不可控/未证明项，不再要求提前证明尚未生成的原片尺寸；生成后检测并记录原片真实尺寸。若原片不严格 9:16，交付片统一采用保持完整画面、无拉伸的等比补边，两组使用同一规则并保留原片；原片的 NOT_PROVEN/不匹配记录不得改为 PASS。不可解码、归属不明或处理失败仍停止，不重新付费生成。此变更不涉及人物/商品/口播绑定、预算、权限、防重、提交不明停止或结果关联门禁。

建议配对设置为播客-女声/普通话、字幕开启、智能适配、AI 自动文案关闭；声音使用当前合同已允许的可见名称/风格读回，不把不可见 `voice_id` 作为新增前置。Owner 确认后才接入对应任务期望和真实字段读取，并完成权限/设置/费用边界回归及独立 Review。回滚为恢复现行比例阻断规则，保留所有原始证据和作品，不迁移或删除历史记录。本候选尚未实施此政策。

其他硬缺口与解除条件：完整阶段二报价及计费单位需要新的当前观察；因果回执没有可复用的当前记录，需要从授权范围内的页面/已有作品或可见网络证据确认结构后接线，不能使用列表第一条或合成 fixture。若只读证据仍不足、必须付费观察，则先报告数量与预算，不增加隐藏冒烟。P1/P3、人物 B 和本轮素材使用/上传范围需要 Owner 指定。本次 30 分钟窗口已结束，新的真实观察需另行限时授权；离线任务继续使用既有 R1 范围。

## 2026-09-07 实现与离线验证历史

以下为此前实现轮次，不覆盖上面的 2026-09-08 校准授权与真实访问记录。

状态：R0 PASS；R1 PARTIAL；R2 BLOCKED（尚未冻结）。Owner 于 2026-09-07 授权隔离研发、零 Provider 测试、Review、Issue/Draft PR；没有授权新上传、付费、合并或部署。

## 基线（2026-09-07 实时核对）

| 项目 | 已核验事实 |
| --- | --- |
| 仓库 | `JettxonHo/hifly-hands-on-product-batch`，默认分支 main |
| 原目录 | `/Users/ketchup/Documents/Product Recommendation clip`，`gui/visual-refresh@6f40cd23889523d86bacdfc61540a41b5b7f9eb6`；ahead 4 / behind 12，存在他人/历史未提交改动，未改动这些文件 |
| 本轮工作目录 | `/Users/ketchup/Documents/hifly-fast-mvp`，独立 `codex/hifly-fast-mvp`，起点干净 |
| 开发基准/main | `2af015ab220ecc9d65de209ea690adb98cacc1c4`，通过 git ls-remote 与 fetch 核验 |
| main CI | [33636720283](https://github.com/JettxonHo/hifly-hands-on-product-batch/actions/runs/33636720283)，success；不是本轮候选 CI |
| 部署来源 | SSH 只读核对 `/opt/hifly-pilot` HEAD 与 App/Worker OCI revision 均为 `3e53bffdd2c59e401815148ccd317b669d32216b`，部署源码工作树干净 |
| 运行状态 | App、Cloud Executor、PostgreSQL、Proxy 四服务 healthy；这不证明飞影登录或当前生产可用 |
| main 与部署差异 | 后续两个提交只变更 README 和配图；不能把旧 GUI 缺功能推断为线上缺功能 |
| 相关未结事项 | #278 合同代码已通过 #279 合并且已部署，但 Issue 仍 OPEN；#273/#272/#263/#257/#252 等仍 OPEN；#258 为唯一 open Draft PR，本轮不关闭或修改它们 |

旧单条校准目录 `/private/tmp/hifly-single-video-verification-20260907` 的未提交脚本与文档保持原状；不把诊断脚本带入正式生产路径。

## 历史 H0（只作为校准证据）

`HIFLY-CAL-20260907-001` / `BEAR-20260907-001` 使用 Playwright 核心 + Computer Use 混合路径。人物来自既有 RBV 私有测试人物素材，商品为 Owner 玩偶图。手持图、视频各提交一次；本地原片 34,822,649 bytes、11.2 秒、1600×2848。可核验记录在原目录 `docs/audits/2026-09-07-single-real-test-result.md` 与私有 `outputs/single-test-20260907/`。

历史页面余额净变化 613 积分；没有逐项账单，也没有可靠远端任务 ID。H0 没有正式工作台 ProductionOrder/Attempt，不能计入 A/B；1600×2848 不是严格 9:16；原 1000 积分授权已耗尽且不继承。本轮 Provider 请求/上传/生成/积分均为 0。

## R1 实现合同（Issue #284）

1. 人物：Worker 通过既有 Asset Store 只读取得合同内的 MaterialVersion，验证组织、注册绑定、可用权限、媒体类型、大小与既有 checksum。V1 失败不回退旧 mapping；不嵌入私有人物原图、不新增资产系统。
2. 口播：新增当前 ready 商品版本的首个人工草稿入口，复用 nullable generation_job_id、draft、幂等、版本和权限。人工来源使用真实确定性检查，并以 finding、接受理由、Review 如实记录 Owner 的语义判断；不使用 controlled_test_double，不伪造自动语义 PASS，编辑保留来源语义。
3. 页面：保留 H0 证实必要的编辑态；绑定本次商品和人物上传。缺少活浏览器、旧图或设置证据时停止。
4. 费用和归属：Cloud 将未知费用写成两项 0 的错误改为 UNKNOWN；正式逐阶段预算和可靠提交回执仍须闭合。只新增一个列表作品不能证明 direct_submission。

## 角色与范围

- 主控负责产品方向、文档、接口任务合同、独立 Review 与最终授权申请。
- 实现者使用准确自定义角色 `luna-worker`；配置文件已核对为 gpt-5.6-luna / max，配置状态 CONFIG_VERIFIED，运行时模型 UNVERIFIED_RUNTIME_MODEL。
- 人物包链路与直接口播入口先分开调查，文件责任由主控派发；测试限制在本仓库，避免跨项目重载。
- Owner 是唯一产品、预算、阶段授权决策者；独立代码审查不新增人工业务审批人。

## 验证与外部行为记录

- Git/GitHub 与部署：只读核对；没有读取凭据、启动飞影浏览器、发出 Hifly 请求或改生产数据。
- 已按授权创建 [Issue #284](https://github.com/JettxonHo/hifly-hands-on-product-batch/issues/284)，是明确的外部写入。
- 锁定依赖安装成功：`npm ci --ignore-scripts --no-audit --no-fund`，250 packages；不加载 .env、不执行 lifecycle scripts。
- 首轮治理回归为 30 tests / 22 pass / 8 fail / 0 skip。失败是旧测试断言 RBV/#278 为现行方向；按 D-038 调整当前断言并保留历史合同保护。它不是业务代码失败，也不记为全绿。
- 主控独立复核人物相关四文件：`assets-service`、`local-agent-package-compiler`、`cloud-executor-playwright`、`cloud-executor-deployment`，74/74 pass、0 skip，日志 `/tmp/hifly-fast-mvp-assets-review.log`。
- 主控独立复核文案相关六文件：`copy-generation-service/api`、`copy-quality-evaluators/service/one-attempt`、`operator-workspace-service`，111/111 pass、0 skip，日志 `/tmp/hifly-fast-mvp-copy-review.log`。以上均使用 `env -i` 清空生产环境变量，只保留 PATH/HOME/TMPDIR，再运行 Node 测试，不加载生产配置文件。
- 实现者报告 Stage 2 浏览器 3/3、Stage 5 7/7；主控已查看人工输入页面截图，尚需补完整浏览器 reload 的未决请求恢复和初次保存状态文案后再复核。
- 本机 Docker daemon 不可用，也没有本地 PostgreSQL 二进制；没有启动全局 Docker、没有改用生产数据库。数据库新增回归待 required CI 的隔离 PostgreSQL 执行；本地不把 skip 当 pass。
- 独立 Review：AUXILIARY_REVIEWER 已读取实际全差异，结论 APPROVED；人物/Cloud/compiler 独立 74/74，文案域/API 独立 84/84、语法与 diff check 通过。主控另行验证，次数不与 Reviewer 计数叠加。Review 修正：Decision Log 更新时间、付费手持图后 abort 立即进入需处理终态、工单未决请求真正跨页面 reload 保留。
- 第一轮全量：`node --test --test-concurrency=2 --test-reporter=spec test/*.test.js`，1392 tests / 1372 pass / 3 fail / 17 skip，自然 exit 1、160234ms，日志 `/tmp/hifly-fast-mvp-full.log`。失败为一个旧默认动作期望、两个 Stage 4 在 URL 先更新时过早读取焦点/弹窗状态的测试。
- 修复不改 Stage 4 生产代码、不加固定 sleep、不弱化断言；等待真实渲染与焦点/弹窗关闭。定向 Project API 21/21、Stage 4 浏览器 9/9。新增 paid-asset abort 即时状态回归后 core 108/108、治理 30/30。
- `npm run check`：254 JavaScript 文件通过；`npm run validate`：3 行仓库示例通过（不是三个真实商品测试）；`git diff --check` 通过。
- 第二轮全量（并发 2、每测试文件限时 120s）：1390 tests / 1366 pass / 4 fail / 3 cancelled / 17 skip，319279ms；4 fail 是新增治理测试把 R1 IN_PROGRESS 写死，与本轮真实 PARTIAL 状态冲突，已改为当前文档一致性校验。3 cancelled 是未修改的 Assets、AvatarSelection、Operator slice A 浏览器文件超时，不能记为产品成功或全绿。
- 三个超时文件独立串行运行：11/11 pass、0 skip，18525ms；没有修改这些页面代码或测试，也未发现本轮工作目录遗留 Node/Chrome 进程。此结果表明本地全量并发验证不稳定，未证明根因，不能用 isolated pass 覆盖全量超时。
- 串行全量仍为 1393 tests / 1375 pass / 0 fail / 1 cancelled / 17 skip，360679ms；Assets 浏览器文件仍超时，故不能归因于并发或宣称全量通过。
- 有界清理探针显示 `root before/after → app before → timeout`，浏览器 close 尚未执行。四个旧 fixture 分别注册 root/app/browser 清理，造成不安全顺序；已统一为 browser → app → root，错误仍报错，不吞失败。只改测试设施，不改产品代码。受影响四文件加显式登录 smoke 为 12/12，Assets 单文件 3/3；临时诊断输出未留在源码。
- Windows CI 的两处新增路径断言错误也已修复：不把反斜杠塞入正则，直接核对完整 `path.join` 结果；POSIX 权限断言仅用于 POSIX，Cloud 生产为 Linux，不把 Windows chmod 当 ACL 证明。
- [Draft PR #285](https://github.com/JettxonHo/hifly-hands-on-product-batch/pull/285) 已建立，未合并。首次 CI [34123651278](https://github.com/JettxonHo/hifly-hands-on-product-batch/actions/runs/34123651278) 的 Ubuntu 通过、Windows 两个路径断言失败；全部数据库步骤（含新 Copy integration）通过，但 identity browser smoke 未结束，主控保留日志后取消该 run。Windows Capture stress [34123651383](https://github.com/JettxonHo/hifly-hands-on-product-batch/actions/runs/34123651383) 通过。
- 清理修正后的本机全量自然 exit 0：1393 tests / 1376 pass / 0 fail / 0 cancelled / 17 skip，129647ms，日志 `/tmp/hifly-fast-mvp-full-cleanup-fixed.log`。17 项环境跳过不是通过；显式登录 smoke 和新增数据库回归分别由定向本地与 CI 验证。
- 实现提交树在 `34a859b38625a2eafd09ee5a3ccaf595a8b32026` 通过 exact-head [CI 34125677649](https://github.com/JettxonHo/hifly-hands-on-product-batch/actions/runs/34125677649)：Ubuntu、Windows、identity-postgres 全部 SUCCESS；新增 Copy generation/quality 数据库步骤与登录 smoke 均实际执行成功。[Windows stress 34125677675](https://github.com/JettxonHo/hifly-hands-on-product-batch/actions/runs/34125677675) SUCCESS。其后只记录验证文字，没有生产代码修改；PR 当前头的状态始终以 [Checks](https://github.com/JettxonHo/hifly-hands-on-product-batch/pull/285/checks) 为准，不把旧 run 冒充新提交结果。
- 对 `e43eca1` 的测试清理/Windows 修复及 `34a859b` 的证据文档已完成第二次独立 Review，APPROVED；无待处理代码 Review 阻断项。R1 PARTIAL 是真实接口/报价控制的能力缺口，不是把离线测试当真实业务通过。
- 官方 registry 生产依赖审计：0 critical、1 high、3 moderate（按包计数），没有修改依赖。high 为 fast-uri，来自 Fastify 的 schema 编译/序列化依赖；本轮未证明应用有受攻击者控制的 URI 解析/SSRF 触发链，不据此宣称风险消失。既有 #157 的公开发布/TLS/依赖风险保留，本轮不执行广泛升级；发布前复核。日志 `/tmp/hifly-fast-mvp-dependency-audit.json`。
- 合并、部署、正式 A/B、真实上传和生成：NOT_EXECUTED。
- 费用：新的 Hifly 提交 0；外部付费模型调用 0；扣费 0。GitHub/SSH 元数据读取不能描述成完全无外部访问。

## 接力

唯一主线见 [ROADMAP](../../ROADMAP.md)。先完成最小可审查候选和零积分回归，再集中提出剩余校准/素材/发布授权。未取得证据的字段继续标记 UNKNOWN/BLOCKED。

## 剩余硬阻断与具体下一步

1. 当前 voice/subtitle/target 的真实读取与设置验证没有接到生产路径；不能用结构化 Mock verifier 代替。
2. 已加入 formal receipt 能力的上传前停止与因果回执校验入口，但真实 receipt parser/resolver 尚未实现/接线，不能把列表差值当作任务归属。legacy 行为只为兼容历史，不能作为 B 测试捷径。
3. Cloud 费用已由伪零改为 null/UNKNOWN；两段实时报价、逐条/阶段上限与持久付费控制仍需完成。历史 H0 613 不是新报价。
4. P1/P3、另一位已验证人物 B、素材重用和本轮上传权限仍需 Owner 指定；实验代码/部署、账号、声音/字幕与报价未冻结。

拟集中请求：一次最多 30 分钟的零积分飞影页面校准，使用 H0 同一云端账号并重新核对身份/会话；读取当前声音、字幕、大小/比例控件、计费说明和已有作品标识/关联机制，必要的非付费设置选择及还原纳入该授权。页面访问可能产生遥测、Profile 缓存和设置写入，均单独记录。新素材上传、新任务创建、生成、数字人创建/克隆、付费模型、视频下载、merge/deploy 均为 0。尚未执行或取得这项新授权。

先用校准结果完成真实接线与预算控制，再冻结六条方案并一次提交 R3 的逐条/总上限请求；缺乏报价与确切素材时不提交空泛消费申请，也不额外补拍。

回滚：本候选无数据库 migration，按独立代码/测试提交审查，可在未发布分支修正；若未来批准发布后需回滚，恢复上一经验证 App/Worker 构建，保留已有订单/审核/付费 checkpoint/素材/结果，不删除历史数据或尝试重新生成。
