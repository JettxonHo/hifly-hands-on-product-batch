# 2026-08-10 第五次真实 Local Agent：飞影手持图生成失败

## 授权与前置状态

- Owner 明确授权最多执行 1 条真实飞影生成，接受积分扣除；失败立即停止且不自动重试。
- 旧失败工单和 attempt 保持不可变，没有恢复、覆盖或重新领取。
- 正式 API 创建 reproduction 工单 `5ce1cd6d-fa17-4d66-acc0-697cf649dc36`。
- 交接包 `5d7d7722-3726-4abe-a608-a551862dc2d8` 为 `ready / v1`；生成 job `46bdad33-675f-4e07-81c3-b2284cece50f` succeeded，attempts 1。
- 只读数据库检查确认该工单是全组织唯一待执行工单，execution attempt 数为 0。
- 本机飞影预检返回 `ready`；预检没有领取工单、上传素材或点击生成。

## 唯一真实执行

- heartbeat、claim、start、交接包下载和租约心跳均成功。
- 人物图和商品图上传成功，商品主图校验通过。
- 自动化只点击了一次手持商品图 `立即生成 150积分`，没有再次点击或自动重试。
- 飞影显示“生成中，请耐心等待”约 30 分钟，随后弹窗明确变为“生成失败”。
- 没有提交外层视频生成，没有下载视频，也没有创建云端候选作品。

## 云端收口

- attempt：`2aaea76b-d770-4e5e-bc89-a486a2b7cb79`
- report：`3b33138b-40dd-4af4-b708-291a3e5e23a3`
- order / attempt：`failed`
- report：`failed / not_retryable`
- candidate：0
- Work：0
- 真实进程退出码：1
- 失败后没有发起第二条真实命令。

## 诊断与无积分修复

飞影的失败弹窗仍包含“再次生成 / 重新编辑 / 确认”。旧识别逻辑仅检查这组按钮，因此把失败弹窗当成成功结果，继续尝试关闭弹窗并进入文案阶段，最终产生误导性的“AI 文案开关未关闭”错误。

修复保持在页面适配器的既有状态边界内：

- 可见手持商品图弹窗新增 `failed` 状态，文本包含“生成失败”时优先判失败。
- `ready` 只有在不是失败态且满足成功动作组或生成图片证据时才成立。
- 等待器检测到失败后立即抛出固定安全错误，不再点击确认、填写文案或提交视频。
- 回归测试覆盖“失败弹窗同时带成功按钮组合”与“等待器立即终止”两个行为。

## 积分与下一步

本轮已点击手持商品图生成，因此存在真实积分扣除。失败截图显示余额 `56191`，但上一轮外层视频可能延迟结算，不能用该数字精确推断本轮扣分；最终以飞影后台账单为准。

本次授权已使用，当前报告不可重试。修复合并后若继续真实验收，必须创建新的 reproduction 工单与 ready 交接包，重新完成唯一待执行和登录预检，并由 Owner 再次明确授权最多 1 条真实生成。
