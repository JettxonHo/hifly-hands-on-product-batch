{
  "name": "hifly-hands-on-product-batch",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser automation workflow for Hifly one-click 'hands-on product' batch video production.",
  "scripts": {
    "login": "node src/login.js",
    "validate": "node src/validate-products.js",
    "run": "node src/run-batch.js",
    "check": "node --check src/*.js scripts/*.mjs",
    "package": "node scripts/package-artifacts.mjs"
  },
  "dependencies": {
    "playwright": "^1.61.0"
  },
  "devDependencies": {}
}
