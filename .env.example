# Copy to .env or export these variables before running optional helpers.
# The browser automation itself reads config.local.json by default.

HIFLY_CONFIG=config.local.json

# Optional: set this when using GitHub CLI in CI or a clean machine.
# GH_TOKEN=
